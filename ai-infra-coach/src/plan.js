const deep = (label, kind, answer) => ({ label, minutes: 0, kind, answer });
const fragment = (label, answer) => ({ label, minutes: 20, answer });

export const phases = [
  {
    id: 1,
    title: "Harness 基础",
    range: "Day 1-7",
    outcome: "能讲清 Agent Runtime、工具调用、MCP 和主流框架抽象。",
  },
  {
    id: 2,
    title: "可靠执行",
    range: "Day 8-14",
    outcome: "掌握规划、上下文、记忆、恢复、安全与人机协同。",
  },
  {
    id: 3,
    title: "Agent 评测",
    range: "Day 15-21",
    outcome: "能设计评测集、指标、Judge、归因和回归体系。",
  },
  {
    id: 4,
    title: "生产与面试",
    range: "Day 22-30",
    outcome: "能回答生产化、优化、多 Agent 与系统设计高频题。",
  },
];

const rawPlan = [
  {
    day: 1,
    phase: 1,
    title: "Agent Harness 到底是什么",
    objective: "先建立 Agent、Workflow、Harness、Runtime 四个概念的边界。",
    deep: [
      deep("画出用户请求到最终答案的 Agent 执行主链路", "理解", "主链路应包括：用户消息 → 模型决策 → tool call → Harness 校验并执行工具 → tool result 回填 → 模型继续决策 → 终止。Harness 负责把概率模型包进一个可控制、可观测、可恢复的执行系统。"),
      deep("比较 Agent、固定 Workflow 与普通 Chatbot", "辨析", "Chatbot 主要生成文本；固定 Workflow 的步骤由代码预先决定；Agent 由模型在运行时选择下一步和工具。生产系统常把确定性流程与 Agent 决策混合，而不是让模型控制一切。"),
      deep("整理 Harness 的六类核心职责", "口述", "至少覆盖模型适配、工具注册与执行、状态与上下文、循环和终止、可靠性与安全、轨迹与评测。回答时先给定义，再沿一次请求说明职责。"),
    ],
    fragments: [
      fragment("闪卡：Agent / Workflow / Harness", "Agent 决定下一步；Workflow 预定义步骤；Harness 承载并约束 Agent 的运行。"),
      fragment("口述：为什么模型 API 不等于 Agent", "模型 API 只提供一次推理；Agent 还需要循环、工具、状态、终止、安全和观测。"),
      fragment("回顾：Harness 的核心价值", "把不稳定的模型决策转化为可执行、可约束、可排障的系统行为。"),
    ],
    deliverable: "一张 Agent 执行链路图 + 2 分钟定义口述",
  },
  {
    day: 2,
    phase: 1,
    title: "Function Calling 与工具 Schema",
    objective: "理解模型如何提出结构化工具调用，以及 Harness 为什么必须校验。",
    deep: [
      deep("解释 Function Calling 的完整数据流", "理解", "Harness 把工具名称、描述和 JSON Schema 随消息发给模型；模型返回工具名与参数；Harness 解析、校验并执行，再用关联 ID 把结果作为 tool message 回填。模型只是提出调用，不会亲自执行工具。"),
      deep("拆解 name、description、parameters 的作用", "Schema", "name 用于稳定选择；description 说明适用场景和边界；parameters 用 JSON Schema 约束类型、properties、required、enum 等。Schema 既是接口契约，也会影响模型选工具和填参数的准确率。"),
      deep("整理参数错误的处理顺序", "可靠性", "先做 JSON 解析和 Schema 校验，再做业务校验、权限检查、幂等判断，最后执行。可修复错误应作为结构化结果回填让模型重试，危险或不可恢复错误应停止或转人工。"),
    ],
    fragments: [
      fragment("闪卡：模型调用工具了吗", "没有。模型只生成 tool call，真正执行发生在 Harness。"),
      fragment("回顾：description 为什么重要", "它是模型理解工具用途、边界和选择条件的主要提示之一。"),
      fragment("回顾：required 与业务校验", "required 只保证字段存在；金额范围、资源归属等仍需业务层校验。"),
    ],
    deliverable: "Function Calling 时序图 + Schema 字段速记",
  },
  {
    day: 3,
    phase: 1,
    title: "MCP 协议与工具发现",
    objective: "能解释 MCP Client、Server、Host、Transport 与 JSON-RPC。",
    deep: [
      deep("画 MCP Host、Client、Server 的关系", "协议", "Host 是承载模型与 Agent 的应用；每个 MCP Client 连接一个 Server；Server 暴露 tools、resources、prompts 等能力。MCP 统一的是能力发现与调用协议，不等于 Agent Runtime。"),
      deep("讲清 initialize、tools/list、tools/call", "时序", "连接后先 initialize 协商协议版本和能力，再发送 initialized 通知；Client 用 tools/list 发现工具，用 tools/call 传入 name 和 arguments。返回结果要与 JSON-RPC request id 对应。"),
      deep("比较 stdio 与 Streamable HTTP", "工程", "stdio 适合本地子进程，进程生命周期和权限边界清楚；Streamable HTTP 适合远程、多客户端和独立部署，但需要鉴权、会话、网络超时与安全防护。传输不同，MCP 消息语义基本一致。"),
    ],
    fragments: [
      fragment("闪卡：MCP 与 Function Calling", "Function Calling 是模型输出工具意图的接口；MCP 是应用与外部能力提供方之间的协议。"),
      fragment("回顾：JSON-RPC 三个关键字段", "请求通常含 jsonrpc、id、method，可选 params；响应用同一 id 关联 result 或 error。"),
      fragment("回顾：stdio 是什么", "父子进程通过标准输入输出交换协议消息，不是把日志随便打印到 stdout。"),
    ],
    deliverable: "MCP 初始化与工具调用时序图",
  },
  {
    day: 4,
    phase: 1,
    title: "Agent Loop 与状态机",
    objective: "把 while 循环讲成有状态、有边界的 Runtime。",
    deep: [
      deep("写出最小 Agent Loop 伪代码", "流程", "循环应包含组装上下文、调用模型、解析响应、执行工具、追加结果和检查终止；异常路径包括解析失败、工具失败、超时、取消和达到预算。生产实现应显式记录每一步状态。"),
      deep("整理终止条件与无限循环防护", "可靠性", "终止包括模型给出 final answer、任务成功判定、不可恢复错误、用户取消，以及 max_steps、时间、token、费用预算耗尽。还应检测重复工具调用、无进展和状态振荡。"),
      deep("区分控制面状态与消息历史", "状态", "消息历史是给模型看的上下文；控制面还包含 step、预算、权限、工具执行状态、重试次数、checkpoint 和 trace id。不能把所有系统状态都塞进自然语言消息。"),
    ],
    fragments: [
      fragment("闪卡：max_steps 够不够", "不够；还要有时间、成本、重复调用和无进展检测。"),
      fragment("回顾：状态机的价值", "让正常、重试、等待、取消、失败和完成路径可枚举、可测试、可恢复。"),
      fragment("回顾：消息不等于全部状态", "消息服务模型推理，控制状态服务执行正确性，两者用途不同。"),
    ],
    deliverable: "最小 Agent Runtime 状态图",
  },
  {
    day: 5,
    phase: 1,
    title: "消息协议与上下文组装",
    objective: "理解 system、user、assistant、tool 消息和 tool_call_id 的关系。",
    deep: [
      deep("追踪一次工具调用对应的消息序列", "协议", "通常是 system/user → assistant(tool_calls) → tool(tool_call_id 对应结果) → assistant(final 或下一次调用)。并行调用时每个结果必须回填到正确的 call id，不能只靠顺序猜。"),
      deep("解释为什么上下文顺序和角色重要", "理解", "模型按序列读取消息，角色帮助区分规则、用户输入、模型行为和外部结果。顺序错误、缺少调用结果或伪造高权限角色都会破坏决策，甚至形成注入风险。"),
      deep("区分原始工具结果与模型可读结果", "设计", "工具可能返回大 JSON、二进制引用或敏感字段。Harness 应保留原始结果供审计，同时裁剪、结构化、脱敏后再放入模型上下文，并明确错误与成功。"),
    ],
    fragments: [
      fragment("闪卡：tool_call_id", "把 assistant 提出的调用与对应 tool result 关联起来，尤其对并行调用很重要。"),
      fragment("回顾：system prompt 是安全边界吗", "不是。它影响模型行为，但不能替代代码级权限、校验和隔离。"),
      fragment("回顾：工具结果为何要裁剪", "减少 token、噪声与敏感信息，同时保留完成下一步所需证据。"),
    ],
    deliverable: "一段完整工具调用消息记录的字段说明",
  },
  {
    day: 6,
    phase: 1,
    title: "ReAct、Plan-and-Execute 与反思",
    objective: "理解常见 Agent 控制模式及其取舍。",
    deep: [
      deep("比较 ReAct 与 Plan-and-Execute", "算法", "ReAct 每步交替推理和行动，能及时利用观察结果但调用次数多、容易局部游走；Plan-and-Execute 先形成计划再执行，适合长任务，但计划可能过时，需要重规划。实际 Harness 常使用混合方式。"),
      deep("解释 Reflection 与 Verifier 的区别", "辨析", "Reflection 通常由模型回顾自身轨迹并提出修正；Verifier 根据规则、测试或独立模型判断结果是否满足条件。自我反思不天然可靠，最好结合外部可验证信号。"),
      deep("说明何时不应暴露长思维过程", "安全", "生产系统更关心结构化决策、工具参数、证据和结果，不应依赖或持久化模型私有长推理。可要求简短 rationale 或计划摘要用于观测，但权限与正确性仍由外部机制保证。"),
    ],
    fragments: [
      fragment("闪卡：ReAct", "在推理、行动、观察之间循环，让下一步利用真实工具结果。"),
      fragment("回顾：反思不是验证", "模型觉得自己对不代表任务真的通过；测试和规则通常更可靠。"),
      fragment("回顾：为什么需要重规划", "环境和工具结果会改变初始假设，旧计划可能不再可行。"),
    ],
    deliverable: "三种控制模式对比表",
  },
  {
    day: 7,
    phase: 1,
    title: "Agent 框架如何比较",
    objective: "不用背 API，从抽象、控制权和生产能力比较框架。",
    deep: [
      deep("建立框架比较维度", "框架", "比较模型适配、工具协议、状态图、持久化、流式、HITL、并发、Tracing、评测和部署，而不是只比较示例代码行数。还要看框架把多少控制权交给模型或开发者。"),
      deep("区分 SDK、编排框架与完整平台", "边界", "SDK 提供模型和工具调用封装；编排框架管理图、状态和恢复；平台还包含部署、观测、评测和权限。一个产品可能同时覆盖多层，面试时要按职责说。"),
      deep("回答为什么可能自研 Harness", "取舍", "当执行语义、权限、延迟、持久化或评测需要强定制时，自研核心 Runtime 可能更可控；代价是维护模型兼容、生态集成和大量边缘路径。通常先用框架验证，再替换关键层。"),
    ],
    fragments: [
      fragment("闪卡：比较框架先看什么", "先看状态、控制流、工具、恢复和观测抽象，再看 API 易用性。"),
      fragment("回顾：框架不是能力来源", "框架降低编排成本，但 Agent 质量仍取决于模型、工具、上下文和评测。"),
      fragment("回顾：何时避免重框架", "流程简单、延迟敏感或需要高度可控时，薄 Runtime 可能更合适。"),
    ],
    deliverable: "一页 Agent 框架选型表",
  },
  {
    day: 8,
    phase: 2,
    title: "任务规划与分解",
    objective: "理解规划质量、执行反馈和搜索成本之间的关系。",
    deep: [
      deep("解释好计划的判定标准", "规划", "计划应覆盖目标、依赖、顺序、完成条件和可执行动作，同时允许根据观察更新。过粗无法执行，过细会增加模型调用、误差累积和上下文负担。"),
      deep("比较线性计划、DAG 与动态规划", "算法", "线性计划适合强顺序任务；DAG 可表达依赖和并行；动态规划是在每步根据环境重选动作。Harness 需要显式保存依赖和状态，不能只保留一段自然语言计划。"),
      deep("说明规划失败如何恢复", "恢复", "先区分目标理解错误、缺少信息、工具不可用和局部执行失败；优先局部修订，不要每次从头重做。连续无进展时触发重规划、换策略或人工确认。"),
    ],
    fragments: [
      fragment("闪卡：任务分解的目的", "降低单步决策复杂度，并暴露依赖、验收和可并行部分。"),
      fragment("回顾：计划越详细越好吗", "不是；过细会增加调用成本和脆弱性，应匹配任务不确定性。"),
      fragment("回顾：DAG 能带来什么", "表达依赖、并行执行和局部重试，比纯列表更适合复杂任务。"),
    ],
    deliverable: "规划策略与失败恢复口述稿",
  },
  {
    day: 9,
    phase: 2,
    title: "工具选择、路由与并行调用",
    objective: "理解模型选工具、Harness 调度工具的责任边界。",
    deep: [
      deep("分析工具选择错误的原因", "路由", "常见原因包括工具描述重叠、Schema 含糊、候选过多、上下文缺信息和模型能力不足。应通过缩小候选集、改描述、补负例、两阶段路由和评测定位，而不是只堆 Prompt。"),
      deep("判断工具调用何时可以并行", "并发", "调用之间无数据依赖、无互斥副作用且资源允许时可并行。Harness 应建立依赖图，并为每个调用独立处理超时、错误和结果关联；不能仅因为模型一次返回多个调用就盲目并发。"),
      deep("解释工具数量过多怎么办", "设计", "可按领域或权限先做粗路由，只向模型暴露当前相关工具；也可用检索选择工具 Schema。需要评测工具召回率与误选率，避免路由阶段漏掉正确工具。"),
    ],
    fragments: [
      fragment("闪卡：模型选工具，谁批准执行", "模型提议，Harness 根据 Schema、权限和策略决定是否执行。"),
      fragment("回顾：并行的必要条件", "无依赖、无冲突副作用，并且并发不会突破下游限额。"),
      fragment("回顾：候选工具过多的风险", "上下文膨胀、选择混淆、误调用和更高延迟。"),
    ],
    deliverable: "工具路由错误分类表",
  },
  {
    day: 10,
    phase: 2,
    title: "短期记忆与长期记忆",
    objective: "区分消息历史、工作记忆、情节记忆和语义记忆。",
    deep: [
      deep("建立 Agent Memory 分类", "记忆", "短期上下文保存当前任务；工作记忆保存计划、变量和中间结论；情节记忆保存过去轨迹；语义记忆保存抽取后的稳定事实。不同记忆应有不同写入、检索、过期和权限策略。"),
      deep("解释长期记忆为什么不能全量写入", "质量", "模型输出可能错误、过时或包含敏感信息。写入前应做事实抽取、来源记录、去重、置信度和权限校验，并允许更新和删除，否则错误会跨会话放大。"),
      deep("比较 Memory 与 RAG", "辨析", "RAG 是从外部知识源检索上下文的机制；Memory 更强调与用户或 Agent 历史交互相关的状态与经验。实现上都可能使用向量检索，但数据来源、生命周期和一致性要求不同。"),
    ],
    fragments: [
      fragment("闪卡：工作记忆", "当前任务中的计划、中间变量、约束和未完成项。"),
      fragment("回顾：记忆写入门槛", "有来源、可验证、去重、权限正确，并明确有效期。"),
      fragment("回顾：向量库不等于记忆系统", "还缺写入策略、更新删除、权限、时效和冲突处理。"),
    ],
    deliverable: "Agent Memory 四象限笔记",
  },
  {
    day: 11,
    phase: 2,
    title: "Context Engineering 与压缩",
    objective: "理解上下文窗口有限时如何选择、组织和压缩信息。",
    deep: [
      deep("解释上下文过长的三类问题", "上下文", "上下文过长会增加成本和延迟，重要信息可能被噪声淹没，还可能超过窗口。不是能放下就都应放入；应围绕当前决策保留规则、状态、证据和必要历史。"),
      deep("比较截断、摘要、检索与结构化状态", "策略", "截断便宜但可能丢关键信息；摘要压缩强但会引入失真；检索按需取回但可能漏召回；结构化状态最可控但需维护 Schema。生产系统常组合使用并保留原始轨迹。"),
      deep("说明 Prompt Injection 与上下文边界", "安全", "工具返回和检索文档属于不可信数据，不能与系统指令同权。Harness 应标注来源、限制可执行能力、过滤敏感数据，并在高风险动作前做独立授权。"),
    ],
    fragments: [
      fragment("闪卡：Context Engineering", "为当前决策选择、排序、压缩并隔离最有用的信息。"),
      fragment("回顾：摘要的主要风险", "遗漏约束、扭曲事实，且后续只能基于失真的摘要继续推理。"),
      fragment("回顾：Lost in the Middle", "长上下文中间的信息更容易被忽略，位置和组织也影响利用率。"),
    ],
    deliverable: "上下文压缩策略对比表",
  },
  {
    day: 12,
    phase: 2,
    title: "超时、重试、幂等与恢复",
    objective: "让不可靠的模型和工具调用具备可控失败语义。",
    deep: [
      deep("区分模型重试与工具重试", "可靠性", "模型重试可能得到不同决策；工具重试可能重复产生副作用。必须先分类超时、限流、临时错误和业务错误，再决定是否重试，不能对所有异常统一重跑。"),
      deep("解释幂等键与 exactly-once 幻觉", "分布式", "支付、发信等操作应带稳定 idempotency key，让重复请求返回同一结果。分布式系统通常只能通过去重和状态记录逼近一次效果，不能仅靠 Agent 记得自己调用过。"),
      deep("设计 checkpoint 恢复点", "恢复", "在模型决策后、工具执行前后持久化 step、消息、调用 ID、参数、结果和预算。恢复时根据工具执行状态决定继续、查询结果或补偿，避免整条轨迹从头重放。"),
    ],
    fragments: [
      fragment("闪卡：哪些错误不应重试", "参数错误、权限拒绝和确定性业务失败通常不应原样重试。"),
      fragment("回顾：指数退避加抖动", "逐步拉长重试间隔并随机化，避免故障时同步重试风暴。"),
      fragment("回顾：副作用工具", "会改变外部世界的工具，执行前后必须记录状态并考虑幂等或补偿。"),
    ],
    deliverable: "Agent 错误分类与恢复矩阵",
  },
  {
    day: 13,
    phase: 2,
    title: "权限、沙箱与 Prompt Injection",
    objective: "理解 Agent 安全必须依赖代码边界，而非模型自律。",
    deep: [
      deep("说明最小权限如何落地", "安全", "按用户、任务和工具动态授予最小能力，限制文件路径、网络域名、凭证和操作范围，并让短期凭证自动过期。模型不应直接接触全局密钥。"),
      deep("建立工具风险分级", "策略", "只读低风险操作可自动执行；可逆写操作需要日志和范围限制；删除、支付、发布等高风险操作需要显式确认、审批或双重校验。风险取决于副作用而不只是工具名称。"),
      deep("解释间接 Prompt Injection", "攻击", "攻击指令可能藏在网页、邮件或工具结果中，诱导 Agent 泄露数据或调用危险工具。应把外部内容视为数据，通过权限隔离、数据流控制、动作审批和审计降低影响。"),
    ],
    fragments: [
      fragment("闪卡：Prompt 能否当权限系统", "不能。Prompt 可被误解或注入，权限必须由代码和基础设施强制执行。"),
      fragment("回顾：沙箱限制什么", "文件、进程、网络、系统调用、资源和凭证暴露范围。"),
      fragment("回顾：确认弹窗不是万能的", "频繁确认会让用户麻木；应只在高风险且信息清晰时请求确认。"),
    ],
    deliverable: "工具风险分级与授权策略",
  },
  {
    day: 14,
    phase: 2,
    title: "Human-in-the-Loop",
    objective: "知道什么时候暂停 Agent、向用户提问或请求审批。",
    deep: [
      deep("区分澄清、确认与审批", "交互", "澄清用于缺少目标或参数；确认用于向用户展示即将发生的高影响动作；审批通常涉及组织权限和责任链。三者触发条件、展示信息和恢复方式都不同。"),
      deep("设计可恢复的中断", "状态", "暂停时应持久化当前状态、待决动作、参数、风险和过期时间；用户响应后从明确 checkpoint 恢复。不能依赖某个进程一直存活等待点击。"),
      deep("解释何时应该主动停止", "决策", "目标含糊、权限不足、关键证据冲突、预算不足或高风险动作无法确认时应停止。优秀 Agent 不是永远执行，而是知道何时升级给人。"),
    ],
    fragments: [
      fragment("闪卡：HITL 的三个入口", "补信息、批动作、处理异常或冲突。"),
      fragment("回顾：暂停需要保存什么", "状态、待决动作、风险说明、上下文引用和恢复 token。"),
      fragment("回顾：减少确认疲劳", "风险分级、合并低风险确认，并提供清晰差异与后果。"),
    ],
    deliverable: "人机协同状态机",
  },
  {
    day: 15,
    phase: 3,
    title: "Agent 评测的基本框架",
    objective: "从“看起来能用”转向任务、数据、指标和基线。",
    deep: [
      deep("拆解一套 Agent Eval", "评测", "先定义目标任务和成功标准，再构造代表性数据集，运行固定版本的 Agent，采集最终结果与轨迹，计算指标并做失败归因。评测必须记录模型、Prompt、工具和环境版本。"),
      deep("区分能力评测与回归评测", "辨析", "能力评测探索系统能解决哪些任务，数据可更广；回归评测保护已知行为，样本稳定且每次变更都运行。线上事故和新失败应沉淀进回归集。"),
      deep("解释为什么不能只看最终答案", "轨迹", "最终答案可能偶然正确但过程危险、昂贵或不可复现，也可能答案措辞不同却已正确完成外部任务。应同时检查环境状态、约束、工具轨迹、成本和延迟。"),
    ],
    fragments: [
      fragment("闪卡：Eval 四要素", "任务集、成功判据、运行协议、指标与归因。"),
      fragment("回顾：回归集从哪来", "高频真实任务、历史故障、边界条件和安全案例。"),
      fragment("回顾：为什么固定版本", "否则模型、Prompt、工具或环境变化会混淆结论。"),
    ],
    deliverable: "一页 Agent Eval 设计模板",
  },
  {
    day: 16,
    phase: 3,
    title: "Outcome Eval 与 Trajectory Eval",
    objective: "学会分别评价任务结果和执行过程。",
    deep: [
      deep("比较 Outcome 与 Trajectory", "评测", "Outcome Eval 判断最终环境状态或答案是否满足目标；Trajectory Eval 检查工具选择、参数、顺序、冗余和违规行为。前者更接近用户价值，后者更利于归因，两者不能互相替代。"),
      deep("解释 exact match 为什么常常不够", "指标", "开放回答可能语义正确但表述不同；外部任务的真正结果可能在数据库、文件或测试状态中。优先使用可执行验证器、环境断言或结构化字段，再用语义 Judge 补充。"),
      deep("处理多条正确轨迹", "设计", "不要把金标准写成唯一调用序列，应定义必须满足的约束、允许的工具集合、最终状态和成本边界。只有在顺序本身影响正确性或安全时才严格比较轨迹。"),
    ],
    fragments: [
      fragment("闪卡：Outcome 优先看什么", "最终环境状态是否满足任务，而不只是最后一句话。"),
      fragment("回顾：Trajectory 的价值", "定位错在规划、选工具、参数、执行还是终止。"),
      fragment("回顾：可执行验证器", "用测试、查询或规则直接检查结果，通常比语言 Judge 更稳定。"),
    ],
    deliverable: "结果与轨迹双层评分表",
  },
  {
    day: 17,
    phase: 3,
    title: "成功率、pass@k 与稳定性",
    objective: "掌握 Agent 常用指标及采样口径。",
    deep: [
      deep("解释 success rate、pass@k 与 pass^k", "指标", "success rate 是单次成功比例；pass@k 表示 k 次尝试至少一次成功，衡量可探索上限；pass^k 表示连续 k 次都成功，更强调稳定性。必须同时报告 k、采样参数和样本量。"),
      deep("说明平均分为什么会骗人", "统计", "任务难度不均、少量高频类别或随机采样都可能掩盖弱点。应按任务类型、风险和难度分层，并报告置信区间、失败数和重复运行方差。"),
      deep("构建质量、成本、延迟多目标指标", "权衡", "成功率是主指标，同时记录模型 token、工具次数、总成本、P50/P95 延迟和安全违规。可以设置硬约束后比较成功率，也可画 Pareto 前沿，避免把省钱但失败的方案称为优化。"),
    ],
    fragments: [
      fragment("闪卡：pass@k", "同一任务尝试 k 次，至少一次成功的概率或比例。"),
      fragment("回顾：pass^k", "连续 k 次都成功，衡量系统稳定复现能力。"),
      fragment("回顾：分层报告", "按任务类型、难度、工具和风险拆指标，避免总平均掩盖问题。"),
    ],
    deliverable: "Agent 指标口径速查表",
  },
  {
    day: 18,
    phase: 3,
    title: "LLM-as-a-Judge",
    objective: "理解 Judge 的适用场景、偏差和校准方法。",
    deep: [
      deep("说明何时使用 LLM Judge", "评测", "当答案开放、难以写规则且需要判断相关性、完整性或风格时可用 Judge。可执行任务应优先规则和环境验证，Judge 更适合作为补充而非唯一真值。"),
      deep("列出 Judge 常见偏差", "偏差", "包括位置偏差、长度偏好、自我偏好、措辞敏感、标准漂移和被待评内容注入。可通过随机交换顺序、结构化 rubric、多 Judge、一致性检查和隔离不可信文本缓解。"),
      deep("解释如何校准 Judge", "方法", "抽取代表性样本由人工双标，计算 Judge 与人工的一致率、混淆矩阵和分类型误差；迭代 rubric 后保留固定校准集。高风险结论仍需人工复核。"),
    ],
    fragments: [
      fragment("闪卡：Judge 不是 ground truth", "它也是概率模型，会受提示、顺序和文本风格影响。"),
      fragment("回顾：Rubric 应包含什么", "明确维度、等级定义、正反例和无法判断时的处理。"),
      fragment("回顾：位置偏差怎么测", "交换候选 A/B 顺序重复评分，观察结论是否改变。"),
    ],
    deliverable: "Judge Rubric 与偏差检查清单",
  },
  {
    day: 19,
    phase: 3,
    title: "失败分类与根因归因",
    objective: "把失败定位到模型、工具、上下文、执行或环境层。",
    deep: [
      deep("建立 Agent 失败 taxonomy", "归因", "可按目标理解、规划、工具发现、参数生成、工具执行、观察理解、上下文、终止、安全和环境波动分类。分类应互斥到足以行动，并允许记录首因与后续连锁失败。"),
      deep("区分症状与根因", "排障", "重复调用是症状，根因可能是工具结果含糊、状态未更新或终止条件缺失。应从 trace 找到第一个偏离预期的决策点，再验证如果修正该点能否改变结果。"),
      deep("解释错误归因如何指导优化", "闭环", "工具误选应改候选与描述，参数错应改 Schema/校验，观察错应改结果结构，上下文丢失应改状态与压缩。若所有失败都归为‘模型不行’，评测就无法驱动工程改进。"),
    ],
    fragments: [
      fragment("闪卡：找首个错误点", "沿轨迹找到最早偏离正确执行的步骤，而不是只看最后报错。"),
      fragment("回顾：连锁失败", "前一步错误改变后续上下文，后面的错误可能只是结果而非根因。"),
      fragment("回顾：可行动分类", "每个类别都应对应可能的负责人、证据和修复策略。"),
    ],
    deliverable: "Agent 失败分类树",
  },
  {
    day: 20,
    phase: 3,
    title: "离线评测、在线实验与回归",
    objective: "理解三类评测在研发闭环中的职责。",
    deep: [
      deep("比较离线、Shadow 与 A/B", "实验", "离线评测快且可复现，但覆盖不了真实分布；Shadow 用真实流量旁路运行，不影响用户但成本高；A/B 直接观察用户指标，风险最大，需要护栏和回滚。三者应逐级放量。"),
      deep("设计 Agent 变更的回归门禁", "工程", "每次修改模型、Prompt、工具或策略都记录版本，先跑确定性单测和核心回归集，再跑随机重复评测；设置成功率、安全、成本和延迟阈值，未通过不能发布。"),
      deep("处理数据污染与过拟合", "质量", "反复针对固定测试集调 Prompt 会让结果虚高。应分 train/dev/test，保留隐藏集，定期引入新真实样本，并防止评测答案进入模型上下文或记忆。"),
    ],
    fragments: [
      fragment("闪卡：离线评测优点", "快速、便宜、可复现，适合开发迭代和回归。"),
      fragment("回顾：Shadow 不等于 A/B", "Shadow 不把结果返回用户；A/B 会影响真实体验。"),
      fragment("回顾：发布护栏", "质量、安全、成本、延迟任一越界都应阻止或回滚。"),
    ],
    deliverable: "Agent 发布评测流水线图",
  },
  {
    day: 21,
    phase: 3,
    title: "Coding Agent 与 SWE-bench 评测",
    objective: "掌握代码 Agent 的环境、验证与基准评测高频问题。",
    deep: [
      deep("讲清 Coding Agent 的基本闭环", "Coding Agent", "读取 issue 和仓库 → 搜索定位 → 查看代码与测试 → 编辑补丁 → 执行测试/静态检查 → 根据反馈继续修改 → 输出结果。Harness 负责工作区、命令、超时、补丁和轨迹。"),
      deep("解释 SWE-bench 如何判断成功", "基准", "任务来自真实仓库 issue，在固定版本环境中应用 Agent 补丁并运行测试；通常以 fail-to-pass 测试转绿且 pass-to-pass 不回归为核心。关键是环境可复现和防止测试泄漏。"),
      deep("分析 Coding Agent 常见失败", "归因", "包括定位错文件、没读约束、补丁过拟合公开测试、环境安装失败、命令超时、修改范围过大和停止过早。应同时看测试结果、diff、命令轨迹、成本和沙箱违规。"),
    ],
    fragments: [
      fragment("闪卡：Coding Agent 最可靠的 Verifier", "可复现的测试、编译、类型检查和静态分析。"),
      fragment("回顾：pass-to-pass", "原本通过的测试应继续通过，用于检测补丁引入的回归。"),
      fragment("回顾：为什么要隔离工作区", "代码和命令不可信，需要限制文件、网络、进程和资源。"),
    ],
    deliverable: "Coding Agent 评测口述稿",
  },
  {
    day: 22,
    phase: 4,
    title: "Tracing 与可观测性",
    objective: "能通过一次完整 Trace 定位 Agent 行为和性能问题。",
    deep: [
      deep("设计 Agent Trace 层级", "观测", "根 span 表示一次任务，子 span 覆盖模型调用、工具调用、检索、审批和重试；记录 trace id、step、模型/Prompt/工具版本、token、延迟、状态和错误，但对敏感内容脱敏。"),
      deep("区分日志、指标和 Trace", "基础", "日志记录离散事件与上下文；指标聚合成功率、延迟和成本趋势；Trace 还原单任务跨步骤时序。排障通常从指标发现问题，用 Trace 定位，再查日志细节。"),
      deep("说明如何观测‘无进展’", "行为", "可记录重复工具和参数、状态变化量、相似观察、连续失败和预算消耗。达到阈值后触发反思、换策略或终止，并将该信号纳入评测。"),
    ],
    fragments: [
      fragment("闪卡：每个 step 至少记录什么", "输入摘要、决策、调用、结果、状态变化、耗时、成本和错误。"),
      fragment("回顾：Trace 为什么要版本信息", "否则无法把行为变化归因到模型、Prompt、工具或策略。"),
      fragment("回顾：观测数据也要脱敏", "Prompt、工具参数和结果可能包含凭证、隐私与业务数据。"),
    ],
    deliverable: "Agent Trace 字段字典",
  },
  {
    day: 23,
    phase: 4,
    title: "长任务、持久化与断点恢复",
    objective: "理解分钟级到小时级 Agent 的执行架构。",
    deep: [
      deep("解释长任务为什么不能绑死 HTTP 请求", "架构", "连接会超时、进程会重启，任务还可能等待人工或外部系统。应返回 job id，由持久化状态机或任务队列驱动，客户端通过轮询、SSE 或通知获取进度。"),
      deep("区分 checkpoint、event log 与 snapshot", "状态", "event log 保存发生过的事件便于审计和重放；snapshot 保存某时刻完整状态便于快速恢复；checkpoint 是可安全继续执行的边界，通常引用两者。"),
      deep("处理取消与补偿", "可靠性", "取消要传播到排队、模型和工具层；已发生的副作用无法靠停止撤销，需要定义补偿动作或转人工。任务状态应区分 cancelling、cancelled、failed 和 compensated。"),
    ],
    fragments: [
      fragment("闪卡：为什么需要 job id", "让任务生命周期独立于单次网络连接，并支持查询、取消和恢复。"),
      fragment("回顾：重放的风险", "副作用操作可能被重复执行，因此必须使用幂等键和执行记录。"),
      fragment("回顾：补偿不是回滚", "外部副作用往往无法原子撤销，只能执行语义上的反向操作。"),
    ],
    deliverable: "长任务状态与恢复图",
  },
  {
    day: 24,
    phase: 4,
    title: "并发、限流与成本预算",
    objective: "在吞吐、延迟、下游配额和单任务预算之间做控制。",
    deep: [
      deep("区分用户并发、Agent 并发和工具并发", "并发", "一个用户任务可能产生多个并行工具调用，因此三个层次的并发不同。应分别限制全局任务、单租户任务和单工具调用，避免某个 Agent 放大流量。"),
      deep("设计多维预算", "预算", "预算可包含 max_steps、wall time、token、金额、工具次数和资源用量。每步执行前预估并检查剩余预算，耗尽时返回已有进展或请求用户扩展，而不是突然失控。"),
      deep("解释背压和熔断", "稳定性", "下游变慢时通过队列上限、信号量和拒绝向上游传递背压；连续错误达到阈值后熔断，避免重试持续打爆服务。恢复应经过半开探测。"),
    ],
    fragments: [
      fragment("闪卡：Agent 的流量放大", "一次用户请求可能触发多次模型与工具调用，不能只按入口 QPS 估算。"),
      fragment("回顾：预算检查时机", "任务开始和每个昂贵或有副作用的步骤执行前。"),
      fragment("回顾：队列不能无限长", "无限排队会放大延迟、内存和过期任务，最终仍然失败。"),
    ],
    deliverable: "Agent 容量与预算控制清单",
  },
  {
    day: 25,
    phase: 4,
    title: "模型路由与成本优化",
    objective: "理解如何在质量约束下减少模型与工具成本。",
    deep: [
      deep("说明模型路由的常见策略", "优化", "可按任务类型、风险、上下文长度和历史置信度路由：简单分类和抽取用小模型，复杂规划或失败升级用强模型。路由器本身必须评测错误路由造成的质量损失。"),
      deep("比较缓存、批处理与上下文压缩", "性能", "响应缓存适合确定性或可规范化请求；批处理提高吞吐但增加等待；上下文压缩减少 token 却可能损失信息。优化必须同时看成功率、P95 延迟和单成功任务成本。"),
      deep("解释如何优化工具描述和 Prompt", "实验", "先从失败分类中提出假设，一次只改一个变量，在固定回归集重复评测。Prompt 更长不一定更好，应衡量工具选择、参数正确率、最终成功率和 token 成本。"),
    ],
    fragments: [
      fragment("闪卡：成本指标为什么用每成功任务", "单次便宜但失败重试更多，最终可能更贵。"),
      fragment("回顾：升级策略", "低成本模型不确定或失败时，再路由到能力更强的模型。"),
      fragment("回顾：优化必须守住什么", "成功率、安全和关键延迟 SLO 是硬约束。"),
    ],
    deliverable: "质量-延迟-成本优化框架",
  },
  {
    day: 26,
    phase: 4,
    title: "多 Agent 系统",
    objective: "知道什么时候多 Agent 有价值，什么时候只是增加复杂度。",
    deep: [
      deep("比较单 Agent 与多 Agent", "架构", "多 Agent 可做角色分工、并行探索和独立验证，但增加通信、状态一致性、成本和归因难度。任务可由一个 Agent 加工具完成时，不应仅为概念新颖而拆分。"),
      deep("解释 Supervisor 模式", "编排", "Supervisor 根据状态把子任务路由给专业 Agent，并聚合结果、处理冲突和终止。它可能成为瓶颈和单点，因此路由规则、共享状态与失败恢复要显式设计。"),
      deep("处理 Agent 之间的信息共享", "状态", "共享完整消息会造成上下文膨胀和权限泄露；应通过结构化任务、产出物引用和最小必要上下文传递。每个 Agent 只获得完成职责所需的信息和工具。"),
    ],
    fragments: [
      fragment("闪卡：多 Agent 的必要条件", "存在明确专业分工、可并行探索或独立验证收益。"),
      fragment("回顾：主要代价", "更多模型调用、通信、冲突、状态同步和排障难度。"),
      fragment("回顾：角色 Prompt 不等于专业能力", "能力仍来自模型、工具、上下文和验证机制。"),
    ],
    deliverable: "单 Agent / 多 Agent 选型口述",
  },
  {
    day: 27,
    phase: 4,
    title: "Verifier、搜索与策略优化",
    objective: "覆盖 Agent 岗位中更偏算法的行为优化问题。",
    deep: [
      deep("解释 Generator-Verifier 架构", "算法", "Generator 产生一个或多个候选计划/答案，Verifier 用测试、规则或模型评分，系统选择、修正或继续搜索。收益取决于候选多样性和 Verifier 与真实目标的一致性。"),
      deep("比较 Best-of-N、Beam 与 Tree Search", "搜索", "Best-of-N 独立采样后选优，实现简单但不共享中间信息；Beam 每层保留高分候选；Tree Search 展开动作并用评估选择分支。搜索提升上限，也快速增加 token、工具成本和环境分支管理。"),
      deep("说明何时考虑微调或强化学习", "训练", "当大量稳定轨迹表明错误来自模型策略、Prompt 和编排已到瓶颈，且有足够高质量监督或奖励信号时再考虑训练。格式和工具说明问题通常先用 Schema、Prompt、路由与评测解决。"),
    ],
    fragments: [
      fragment("闪卡：Verifier 的上限", "Verifier 若无法识别真正好结果，生成更多候选也选不对。"),
      fragment("回顾：搜索的核心代价", "分支数带来模型、工具、环境快照和评估成本。"),
      fragment("回顾：先评测再训练", "没有稳定数据和归因，微调只会把未知问题固化。"),
    ],
    deliverable: "Agent 行为优化方法对比表",
  },
  {
    day: 28,
    phase: 4,
    title: "生产发布与版本治理",
    objective: "把模型、Prompt、工具和策略作为一个可回滚系统发布。",
    deep: [
      deep("定义 Agent 版本包含什么", "治理", "至少包含模型及参数、system prompt、工具 Schema/实现、编排策略、记忆与检索配置、依赖和评测集版本。只记录代码 commit 无法复现一次 Agent 行为。"),
      deep("设计灰度与回滚", "发布", "先离线回归，再 Shadow、内部用户、小流量 Canary，持续观察成功率、安全、成本和延迟。保留旧版本及状态兼容方案，越过护栏时自动停止放量或回滚。"),
      deep("处理工具 Schema 演进", "兼容", "新增可选字段通常向后兼容；删除、重命名和语义变化需要版本化。长任务恢复时必须知道原调用使用哪个 Schema，并对旧状态迁移或继续使用旧执行器。"),
    ],
    fragments: [
      fragment("闪卡：一次 Agent 发布包含什么", "模型、Prompt、工具、策略、配置和依赖的共同版本。"),
      fragment("回顾：Canary 看哪些护栏", "质量、安全违规、错误率、P95 延迟和单任务成本。"),
      fragment("回顾：为什么要保存原始 Trace", "用于复现、审计、失败归因和新旧版本对放。"),
    ],
    deliverable: "Agent 发布检查清单",
  },
  {
    day: 29,
    phase: 4,
    title: "Agent 系统设计题",
    objective: "形成需求、架构、可靠性、安全、评测的固定答题顺序。",
    deep: [
      deep("整理 Agent 系统设计答题模板", "系统设计", "先澄清用户、任务、工具、副作用、规模和 SLO；再画 Runtime 主链路与状态；随后讲可靠性、安全、观测、评测和成本；最后给容量估算、取舍与演进。"),
      deep("设计一个桌面操作 Agent", "案例", "应覆盖感知/工具层、计划与执行、权限和确认、沙箱、状态持久化、失败恢复、轨迹评测及多平台适配。高风险桌面动作必须由代码层约束。"),
      deep("回答自研还是用框架", "取舍", "根据交付速度、控制流复杂度、恢复与权限需求、团队维护能力决定。可先用框架验证，保持模型/工具接口隔离，再逐步替换性能或控制不足的核心层。"),
    ],
    fragments: [
      fragment("闪卡：系统设计第一步", "先澄清任务、规模、SLO、风险与约束，不急着报框架名。"),
      fragment("回顾：Agent 题必须主动讲什么", "不确定性、终止、权限、恢复、观测和评测。"),
      fragment("回顾：如何体现后端优势", "状态机、幂等、队列、权限、可观测性和生产故障处理。"),
    ],
    deliverable: "15 分钟 Agent 系统设计模板",
  },
  {
    day: 30,
    phase: 4,
    title: "Harness 面试总复盘",
    objective: "把 150 道题压缩成可稳定口述的主干知识图。",
    deep: [
      deep("闭卷讲完一次 Agent 请求", "复盘", "从消息与工具 Schema 开始，讲模型决策、工具校验执行、状态更新、上下文、终止，再补超时重试、权限、Trace 和 Eval。目标是形成一条贯穿全部知识点的主链路。"),
      deep("用失败案例串起可靠性和评测", "面试", "选一个工具误调用或循环案例，说明如何通过 trace 定位首因，怎样修复 Schema/状态/策略，如何加入回归集，并用成功率、成本和延迟验证没有副作用。"),
      deep("完成一次 30 分钟模拟面试", "口述", "覆盖定义、MCP、Agent Loop、上下文、可靠性、安全、Eval 和系统设计。每题先给结论，再讲机制、取舍与验证；不会的内容明确边界和验证方式。"),
    ],
    fragments: [
      fragment("闪卡：Harness 六条主线", "执行、工具、状态、可靠性、安全、观测评测。"),
      fragment("回顾：回答八股的四层结构", "定义 → 机制 → 取舍/失败 → 如何验证。"),
      fragment("回顾：你的岗位定位", "以后端工程为底座，向 Agent Runtime、可靠性与评测优化迁移。"),
    ],
    deliverable: "Agent Harness 知识图 + 模拟面试记录",
  },
];

export const plan = rawPlan.map((day) => {
  const deepTasks = day.deep.map((task, index) => ({
    ...task,
    minutes: index === 2 ? 30 : 35,
  }));
  return { ...day, deep: deepTasks };
});

export const taskId = (day, bucket, index) => `${day}-${bucket}-${index}`;
