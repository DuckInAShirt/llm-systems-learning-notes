const q = (question, answer) => ({ question, answer });

export const extraQuestions = {
  1: [
    q("TTFT、ITL 和 TPS 分别衡量什么？", "TTFT 是首 token 延迟，ITL 是后续 token 间隔，TPS 是单位时间生成的 token 数。"),
    q("在线推理和离线推理的目标有什么不同？", "在线推理更关注单请求延迟和尾延迟，离线推理更关注整体吞吐和 GPU 利用率。"),
    q("为什么生成式推理不能一次并行得到全部输出？", "每个新 token 都依赖此前已经生成的 token，因此 decode 必须自回归逐步执行。"),
    q("吞吐和延迟一定同时变好吗？", "不一定。扩大 batch 常提高吞吐，但也可能增加请求等待和单请求延迟。"),
  ],
  2: [
    q("KV Cache 保存的是什么？", "保存每层历史 token 的 Key 和 Value，避免 decode 时重复计算已有前缀。"),
    q("KV Cache 大小与哪些因素有关？", "主要与层数、KV head 数、head dimension、序列长度、batch 和数据类型有关。"),
    q("MHA、GQA、MQA 的 KV Cache 大小有何区别？", "它们的 Query head 可以相同，但 KV head 逐渐减少，因此 GQA 和 MQA 的缓存更小。"),
    q("为什么 KV Cache 会限制最大并发？", "每个活跃请求都会持续占用并增长 KV Cache，显存耗尽后无法接纳更多 token 或请求。"),
  ],
  3: [
    q("静态 batching 的主要问题是什么？", "一个 batch 往往需要等待最长请求完成，短请求完成后的计算位置会被浪费。"),
    q("什么是 iteration-level scheduling？", "调度器在每个 decode 迭代重新决定哪些请求参与下一次模型执行。"),
    q("调度器为什么要同时考虑 token 数和请求数？", "不同请求长度差异很大，只看请求数无法准确估计计算量和 KV Cache 需求。"),
    q("什么是 head-of-line blocking？", "队首的大请求阻塞后续小请求，使它们即使能快速完成也必须等待。"),
  ],
  4: [
    q("PagedAttention 中逻辑块和物理块是什么？", "逻辑块属于序列的连续 token，物理块是实际 GPU 显存块，两者通过 block table 映射。"),
    q("分页为什么能减少外部碎片？", "请求不再需要一整段连续显存，任意空闲物理块都可以被分配。"),
    q("PagedAttention 为什么支持前缀共享？", "多个请求的逻辑块可以映射到相同物理 KV block，并通过引用计数管理。"),
    q("block size 越小越好吗？", "不一定。小块减少内部浪费，但会增加元数据、映射和 kernel 管理开销。"),
  ],
  5: [
    q("Benchmark 为什么需要 warm-up？", "首次运行可能包含模型加载、CUDA context、图编译和缓存建立，不能代表稳定性能。"),
    q("P50 和 P99 有什么区别？", "P50 反映典型请求，P99 反映最慢的尾部请求和系统抖动。"),
    q("为什么要报告输入输出长度分布？", "prefill、decode 和 KV Cache 成本都与长度强相关，长度不同的结果不能直接比较。"),
    q("开环和闭环压测有什么区别？", "开环按外部到达率发请求，闭环通常等前一个请求完成再发，更容易掩盖排队和过载。"),
  ],
  6: [
    q("Baseline 应记录哪些环境信息？", "GPU、驱动、CUDA、框架版本、模型、精度、并行配置、启动参数和负载条件。"),
    q("为什么同一命令多次结果不同？", "可能受到 GPU 温度、后台任务、缓存状态、随机请求和调度时序影响。"),
    q("GPU 利用率高就一定性能好吗？", "不一定，可能存在低效 kernel、通信等待或过载，需要结合吞吐和延迟分析。"),
    q("什么情况下实验结果应该作废？", "环境、模型、负载或配置未按设计固定，出现错误重试，或采样数量不足时。"),
  ],
  7: [
    q("推理服务的主要资源瓶颈有哪些？", "计算、显存容量、显存带宽、GPU 通信、CPU 调度和网络都可能成为瓶颈。"),
    q("如何判断瓶颈在 prefill 还是 decode？", "分别观察 TTFT、ITL、输入输出长度和阶段 profiling。"),
    q("为什么要画系统数据流？", "它能明确模块边界、关键状态和指标归属，便于源码阅读与故障定位。"),
    q("第一周最应掌握的因果链是什么？", "请求负载如何经过 prefill/decode，并通过调度和 KV Cache 影响延迟、吞吐与并发。"),
  ],
  8: [
    q("源码阅读为什么要固定 commit？", "代码持续变化，固定 commit 才能保证笔记、行号和实验结果可以复现。"),
    q("如何找到一个请求的源码入口？", "从可运行 API 路由、命令入口或日志栈开始，沿调用关系追踪核心对象。"),
    q("大型项目中数据结构为什么比函数细节更重要？", "关键状态通常保存在少数核心对象中，理解状态流转后才能判断函数的作用。"),
    q("git blame 在源码学习中有什么用？", "可以找到某行代码引入的提交和设计背景，理解为什么这样实现。"),
  ],
  9: [
    q("OpenAI 兼容 API 层主要做什么？", "处理协议、参数校验、模板、序列化、流式响应和错误映射。"),
    q("为什么请求需要内部 ID？", "用于取消、日志关联、状态追踪和将异步结果返回正确客户端。"),
    q("SSE 与 WebSocket 有什么区别？", "SSE 是服务器到客户端的单向 HTTP 流，模型 token 流式返回通常已经足够。"),
    q("客户端断开后服务端应该做什么？", "传播取消信号并尽快释放 Scheduler 和 KV Cache 中的请求资源。"),
  ],
  10: [
    q("Scheduler 每轮主要输出什么？", "本轮要执行的请求、token 数、KV Cache 映射和 Worker 所需元数据。"),
    q("prefill 和 decode 为什么会竞争资源？", "它们共享 GPU 计算、batch token 预算和调度时间，但计算特征和延迟目标不同。"),
    q("抢占请求可能带来什么代价？", "已分配的缓存可能需要释放或重算，增加延迟和额外计算。"),
    q("调度公平性与吞吐如何冲突？", "优先短请求可提高完成数，但长请求可能饥饿；严格公平又可能降低整体效率。"),
  ],
  11: [
    q("KV Cache Manager 管理哪些状态？", "物理块、空闲块、block table、引用计数以及请求与缓存的映射。"),
    q("请求结束时为什么必须及时释放 block？", "否则显存泄漏会逐渐降低可用并发并最终导致 OOM。"),
    q("引用计数的作用是什么？", "判断共享 block 还有多少使用者，只有引用归零时才能安全回收。"),
    q("缓存淘汰策略需要考虑什么？", "命中价值、最近使用、块数量、请求优先级和重新计算成本。"),
  ],
  12: [
    q("Worker 和 Scheduler 为什么通常分离？", "Scheduler 管全局请求状态，Worker 专注设备上的模型执行，便于扩展多进程和多卡。"),
    q("Model Runner 在 forward 前需要准备什么？", "token、position、KV slot、attention metadata 和采样相关信息。"),
    q("CUDA Graph 为什么可能提升推理性能？", "复用已经捕获的 kernel launch 图，减少 CPU 调度和 launch 开销。"),
    q("采样是在模型内部完成吗？", "模型 forward 产生 logits，框架通常在其后的采样模块执行 temperature、top-k 等策略。"),
  ],
  13: [
    q("AllReduce 的输入输出是什么？", "每个 rank 提供同形状数据，完成规约后每个 rank 都得到相同的规约结果。"),
    q("AllGather 与 ReduceScatter 有什么差别？", "AllGather 收集各 rank 分片，ReduceScatter 先规约再把结果分片给各 rank。"),
    q("TP 与 DP 的区别是什么？", "TP 切分单个模型层，DP 复制模型并处理不同请求或数据。"),
    q("NCCL 为什么需要了解 GPU 拓扑？", "PCIe、NVLink 等连接带宽不同，通信算法和 rank 排布会影响实际性能。"),
  ],
  14: [
    q("vLLM 主链路包含哪些核心模块？", "API Server、Engine Core、Scheduler、KV Cache Manager、Worker、Model Runner 和输出处理。"),
    q("如何把实验现象映射到源码？", "从变化的指标定位阶段，再寻找对应调度、缓存或执行路径和配置。"),
    q("源码讲解应该包含什么？", "入口、核心对象、状态变化、关键分支、输出和设计取舍。"),
    q("为什么不要背源码行号？", "行号会随版本变化，应该记模块职责、对象关系和可重复的查找路径。"),
  ],
  15: [
    q("实验矩阵过大应该怎么办？", "先用小规模筛选敏感变量，再对关键区域增加采样，而不是穷举所有组合。"),
    q("为什么原始结果不能只保存在终端？", "终端输出难追踪环境和配置，也不利于自动分析、复现和审计。"),
    q("配置文件应包含哪些内容？", "模型、硬件、精度、并行、负载、随机种子、软件版本和启动参数。"),
    q("如何减少手工实验错误？", "用脚本生成命令、采集环境、命名结果并校验必需字段。"),
  ],
  16: [
    q("什么是系统饱和点？", "继续增加负载时吞吐增长很小，但排队和尾延迟开始快速上升的位置。"),
    q("并发和 batch size 是同一个概念吗？", "不是，并发是活跃请求数量，实际每轮 batch 由 Scheduler 根据资源动态组成。"),
    q("为什么要观察 P99 而不只看平均值？", "平均值会掩盖少量严重超时，而线上体验和 SLO 往往受尾延迟决定。"),
    q("如何选择生产并发上限？", "在满足延迟 SLO 和稳定余量的前提下选择，而不是直接使用最大吞吐点。"),
  ],
  17: [
    q("长 prompt 为什么增加 TTFT？", "prefill 需要处理更多 token，Attention 和线性层计算量随输入增加。"),
    q("长输出为什么降低系统并发能力？", "请求驻留更久并持续占用增长的 KV Cache，周转率下降。"),
    q("相同总 token 数的请求性能一定相同吗？", "不一定，输入和输出分配不同会落在不同阶段，调度和缓存成本也不同。"),
    q("为什么长度实验要固定请求分布？", "随机长度波动会混淆目标变量，使结果无法准确归因。"),
  ],
  18: [
    q("TP 增加后单卡显存如何变化？", "模型参数和部分中间状态被切分，单卡模型显存通常下降，但缓存和通信 buffer 仍存在。"),
    q("什么是扩展效率？", "实际加速比除以 GPU 数量，用于衡量增卡后距离线性加速有多远。"),
    q("通信计算比为什么重要？", "计算量不足而通信量较大时，GPU 会花更多时间等待通信，TP 收益下降。"),
    q("A30 没有 NVLink 时应注意什么？", "重点观察 PCIe 通信是否成为瓶颈，并避免默认假设多卡一定加速。"),
  ],
  19: [
    q("Prefix Cache 命中的必要条件是什么？", "新请求必须与缓存请求拥有完全一致的 token 前缀和兼容的模型配置。"),
    q("Prefix Cache 主要优化 prefill 还是 decode？", "主要跳过共享前缀的 prefill 计算，后续 decode 仍需正常执行。"),
    q("Chunked Prefill 解决什么问题？", "把大 prefill 拆成较小块参与调度，避免长期阻塞 decode 请求。"),
    q("Chunked Prefill 的代价是什么？", "增加调度复杂度和多次执行开销，配置不当可能降低单请求效率。"),
  ],
  20: [
    q("权重量化主要节省什么？", "减少模型权重显存和读取带宽，有时也能提高计算吞吐。"),
    q("W4A16 表示什么？", "权重使用 4 bit，激活通常保持 16 bit。"),
    q("量化评测为什么需要质量指标？", "性能和显存改善可能伴随输出质量下降，仅测速度无法判断是否可用。"),
    q("为什么不同 GPU 的量化收益不同？", "硬件指令、计算能力和可用 kernel 不同，量化格式未必都有高效实现。"),
  ],
  21: [
    q("实验报告的结论应该如何表述？", "同时说明条件、变化、证据、机制解释和限制，避免无条件的绝对结论。"),
    q("什么是异常值？能直接删除吗？", "异常值是明显偏离分布的观测，删除前必须找到合理原因并保留记录。"),
    q("图表最少应标注什么？", "指标名称、单位、实验条件、样本量和必要的误差范围。"),
    q("失败实验有没有价值？", "有，它能揭示假设错误、容量边界和环境问题，也是证明真实实践的重要证据。"),
  ],
  22: [
    q("SSE 为什么适合 token 流式输出？", "它基于普通 HTTP 提供单向事件流，实现简单、代理兼容性较好。"),
    q("流式输出如何计算 TTFT？", "从客户端发出请求开始，到收到第一个有效生成 token 的时间。"),
    q("超时应该分成哪些类型？", "连接、排队、首 token、单 token 间隔、总请求和下游工具超时。"),
    q("为什么流式客户端需要处理半包？", "网络读取边界不等于 SSE 事件边界，需要按协议缓冲并解析完整事件。"),
  ],
  23: [
    q("限流、排队和拒绝分别解决什么？", "限流控制进入速率，排队吸收短时突发，拒绝在容量不足时保护系统。"),
    q("什么是 admission control？", "在请求进入核心执行资源前，根据预计成本和容量决定接受、等待或拒绝。"),
    q("为什么需要最大队列长度？", "无限排队会造成不可接受延迟、内存增长和过期请求堆积。"),
    q("过载时如何降级？", "限制输出长度、路由小模型、降低优先级或返回明确的稍后重试。"),
  ],
  24: [
    q("推理服务至少需要哪些指标？", "请求率、错误率、TTFT、ITL、P99、队列、KV 使用率、GPU 利用率和 token 吞吐。"),
    q("日志、指标、Trace 分别适合什么？", "日志记录离散事件，指标观察趋势，Trace 还原单请求跨模块时序。"),
    q("GPU 利用率低可能是什么原因？", "请求不足、CPU 调度慢、通信等待、数据准备瓶颈或 kernel 太碎。"),
    q("如何定位 P99 偶发尖峰？", "关联请求长度、队列、GC/CPU、通信、缓存分配和故障重试的时间线。"),
  ],
  25: [
    q("Readiness 和 Liveness 有什么区别？", "Readiness 判断能否接收流量，Liveness 判断进程是否需要重启。"),
    q("OOM 后为什么不能只重启？", "需要找到容量、请求长度、缓存或内存泄漏原因，否则会持续重启。"),
    q("什么是优雅退出？", "停止接收新请求，等待或取消存量请求，释放通信和 GPU 资源后退出。"),
    q("为什么重试需要指数退避？", "降低故障期间的请求压力，给下游恢复时间并避免同步重试风暴。"),
  ],
  26: [
    q("Kubernetes 如何分配 GPU？", "通常通过设备插件暴露扩展资源，Pod 在 resources limits 中申请 GPU。"),
    q("模型副本扩容为什么慢？", "需要调度 GPU、拉取镜像和权重、初始化模型并进行 warm-up。"),
    q("Ray Serve 与 vLLM 分别负责什么？", "vLLM 负责模型执行和请求调度，Ray Serve 负责副本、路由、部署和扩缩容。"),
    q("为什么 GPU 服务需要预留容量？", "负载突发和故障恢复都需要余量，长期满载会使尾延迟和稳定性恶化。"),
  ],
  27: [
    q("RadixAttention 的核心思想是什么？", "用 Radix Tree 管理和复用不同请求之间的前缀 KV Cache。"),
    q("比较 vLLM 与 SGLang 应固定什么？", "模型、精度、硬件、输入输出分布、并发、请求率、版本和采样参数。"),
    q("为什么框架默认参数不能直接公平比较？", "默认 batch、缓存、内存比例和调度策略可能不同，造成额外变量。"),
    q("PD 分离解决什么问题？", "把计算密集的 prefill 与带宽、延迟敏感的 decode 分开调度和扩容。"),
  ],
  28: [
    q("为什么项目必须锁定依赖版本？", "上游框架行为和默认参数会变化，不锁版本就无法复现结果。"),
    q("README 应先写原理还是运行方法？", "先让用户明确环境和最小运行方式，再提供架构、实验和原理细节。"),
    q("如何处理无法公开的模型或数据？", "提供可替代的公开配置、数据 Schema 和生成脚本，不泄露敏感资产。"),
    q("项目中哪些内容最能证明工程能力？", "自动化、测试、环境检查、错误处理、复现流程和真实故障记录。"),
  ],
  29: [
    q("项目介绍为什么要准备多个时长？", "不同面试阶段允许的时间不同，需要保持主线一致并按追问逐层展开。"),
    q("系统设计题先从哪里开始？", "先明确目标、流量、模型、SLO 和约束，再做容量估算与组件设计。"),
    q("面试中忘记具体数字怎么办？", "说明指标定义和测试方法，诚实表示具体数字需要查记录，不能临场编造。"),
    q("如何回答没有做过的技术？", "明确实践边界，给出当前理解、相近经验以及会如何验证和落地。"),
  ],
  30: [
    q("推理优化的三个主要目标是什么？", "延迟、吞吐和成本，同时还要满足输出质量与稳定性。"),
    q("如何选择下一阶段方向？", "根据实验中最常遇到的瓶颈，选择源码调度、集群平台或 CUDA kernel 深挖。"),
    q("一个月项目最重要的证据是什么？", "可运行代码、原始数据、可复现命令、图表和能解释失败的技术叙事。"),
    q("如何避免再次变成只会调用框架？", "持续追踪请求主链路，用小实验验证机制，并至少修改或实现一个核心组件。"),
  ],
};

export function questionsForDay(day) {
  return [
    { question: day.question, answer: day.answer, required: true },
    ...(extraQuestions[day.day] || []),
  ];
}
