const q = (question, answer) => ({ question, answer });

export const extraQuestions = {
  1: [
    q("Agent Harness 解决什么问题？", "它在模型外管理工具、状态、循环、权限、恢复和观测，把一次概率模型调用变成可控制的任务执行系统。"),
    q("Agent 与 Workflow 有什么区别？", "Agent 由模型在运行时决定下一步；Workflow 的步骤和分支主要由代码预定义。生产系统常把两者混合。"),
    q("Harness 和 Agent Runtime 有区别吗？", "语境中经常混用。Runtime 更强调执行循环与状态，Harness 往往还包括模型适配、工具、安全、Tracing 和评测等外围能力。"),
    q("为什么 Agent 岗位仍然需要后端能力？", "Agent 涉及状态机、队列、并发、幂等、权限、持久化、可观测和故障恢复，模型能力必须通过可靠工程才能交付。"),
  ],
  2: [
    q("Function Calling 的模型会真的执行函数吗？", "不会。模型只生成结构化调用意图，Harness 负责校验参数、检查权限、执行函数并回填结果。"),
    q("工具 Schema 通常包含什么？", "核心是 name、description 和参数 JSON Schema；参数中常用 type、properties、required、enum 和字段说明。"),
    q("为什么工具 description 会影响效果？", "模型依赖名称、描述和参数说明判断何时选工具以及如何填参，描述重叠或含糊会增加误选。"),
    q("模型生成的参数不符合 Schema 怎么办？", "Harness 应拒绝直接执行，返回结构化校验错误；可修复时让模型有限重试，不可恢复或高风险时停止。"),
  ],
  3: [
    q("MCP 的 Host、Client、Server 分别是什么？", "Host 承载 Agent；Client 在 Host 内连接某个 Server；Server 通过协议暴露工具、资源或 Prompt。"),
    q("MCP 与 Function Calling 的关系是什么？", "Function Calling 连接模型和 Harness 的工具意图；MCP 连接 Harness 与工具提供方。Harness 可把 MCP Schema 转成模型工具格式。"),
    q("stdio 和 JSON-RPC 分别是什么？", "stdio 是本地父子进程的传输通道；JSON-RPC 是用 JSON 表示请求、响应、方法和关联 id 的消息协议。"),
    q("MCP 初始化为什么需要能力协商？", "双方可能支持不同协议版本和能力，initialize 先确定可用功能，避免直接调用不支持的方法。"),
  ],
  4: [
    q("最小 Agent Loop 有哪些步骤？", "组装上下文、调用模型、解析响应、校验并执行工具、回填观察、检查成功或终止，然后进入下一步。"),
    q("如何避免 Agent 无限循环？", "设置步数、时间、token 和费用预算，并检测重复调用、状态无变化和连续失败；必要时重规划或转人工。"),
    q("为什么 Agent Runtime 应建模为状态机？", "状态机能显式表达运行、等待工具、等待审批、重试、取消、失败和完成，便于测试、恢复和观测。"),
    q("消息历史能否代表 Agent 的全部状态？", "不能。预算、权限、重试、checkpoint、工具执行状态等控制信息不应只靠自然语言消息保存。"),
  ],
  5: [
    q("tool_call_id 有什么作用？", "它把 assistant 提出的调用与对应 tool result 关联，在并行调用和重试时尤其重要。"),
    q("为什么工具结果要以 tool 消息回填？", "这样模型能区分自身先前输出和外部世界的观察，并基于真实执行结果继续决策。"),
    q("工具原始结果应全部塞进上下文吗？", "不应。应保留原始结果供审计，再按任务需要裁剪、结构化和脱敏，减少噪声、成本与泄露风险。"),
    q("System Prompt 是安全边界吗？", "不是。它只能引导模型，真正的权限、数据隔离和危险操作审批必须由代码和基础设施执行。"),
  ],
  6: [
    q("ReAct 的核心思想是什么？", "让模型在推理、行动和观察之间循环，使下一步能利用真实环境反馈。"),
    q("ReAct 与 Plan-and-Execute 如何取舍？", "ReAct 适合动态环境但容易游走；先规划适合长任务但计划可能过时。常见方案是先粗规划、逐步执行并按反馈重规划。"),
    q("Reflection 与 Verifier 有什么区别？", "Reflection 是模型自我回顾；Verifier 用规则、测试或独立模型判断是否满足标准，通常更接近外部证据。"),
    q("为什么不能把模型思维过程当审计日志？", "内部推理不保证真实、稳定或可获得。审计应记录输入、结构化决策、工具调用、状态变化和结果证据。"),
  ],
  7: [
    q("选择 Agent 框架应看哪些维度？", "看工具协议、状态与控制流、持久化、HITL、并发、Tracing、评测、部署和扩展边界。"),
    q("SDK、编排框架和 Agent 平台有什么区别？", "SDK 封装模型与工具；编排框架管理图和状态；平台进一步提供部署、权限、观测和评测。"),
    q("什么时候适合自研 Harness？", "当执行语义、安全、恢复、延迟或评测需要强定制且团队能承担维护成本时；通常先验证再逐步替换核心层。"),
    q("框架越重能力越强吗？", "不一定。重框架功能多但抽象、调试和依赖成本也更高，应根据任务复杂度和生产需求选择。"),
  ],
  8: [
    q("任务分解为什么能帮助 Agent？", "它降低单步复杂度，显式暴露依赖、可并行部分和验收条件，让失败可以局部恢复。"),
    q("计划应该多细？", "细到每一步可执行和验证即可；过细会增加调用次数、误差累积和上下文成本。"),
    q("线性计划和 DAG 适合什么任务？", "强顺序任务适合线性计划；存在依赖和并行子任务时 DAG 更合适。"),
    q("什么时候需要重规划？", "工具结果推翻假设、资源不可用、连续无进展或目标发生变化时，应局部修订或重规划。"),
  ],
  9: [
    q("工具选择错误通常来自哪里？", "工具描述重叠、Schema 含糊、候选过多、上下文缺失、模型能力不足或路由器漏召回。"),
    q("如何减少模型看到的工具数量？", "先按领域、权限或检索做粗路由，只暴露当前任务相关工具，同时评测正确工具的召回率。"),
    q("多个 tool calls 都能并行吗？", "不能。只有无数据依赖、无副作用冲突且不突破下游配额时才能并行。"),
    q("工具路由应该看哪些指标？", "正确工具召回率、选择准确率、参数正确率、最终任务成功率，以及误调用成本。"),
  ],
  10: [
    q("Agent Memory 有哪些类型？", "可分短期上下文、工作记忆、情节记忆和语义记忆，分别服务当前任务、状态、历史轨迹和稳定事实。"),
    q("长期记忆为什么需要写入门槛？", "模型结论可能错误、过时或敏感，需要验证来源、去重、权限、置信度和有效期。"),
    q("Memory 与 RAG 有何区别？", "RAG 主要检索外部知识；Memory 强调用户和 Agent 历史状态。两者实现可相似，但生命周期和一致性要求不同。"),
    q("向量数据库等于长期记忆吗？", "不等于。还需要写入、更新、删除、权限、冲突和过期策略。"),
  ],
  11: [
    q("Context Engineering 是什么？", "围绕当前决策选择、排序、组织、压缩和隔离规则、状态与证据，使模型获得最有用的上下文。"),
    q("上下文窗口放得下时为什么还要压缩？", "长上下文会增加成本和延迟，噪声还会淹没关键信息，放得下不等于利用得好。"),
    q("摘要压缩有什么风险？", "可能遗漏约束、扭曲事实，后续步骤只能基于有损摘要继续推理。"),
    q("如何防御检索内容里的 Prompt Injection？", "把外部文本视为不可信数据，隔离权限、标注来源、限制工具能力，并对高风险动作独立授权。"),
  ],
  12: [
    q("哪些工具错误适合重试？", "限流、网络抖动和临时不可用等可有限重试；参数、权限和确定性业务错误不应原样重试。"),
    q("为什么有副作用的工具不能盲目重试？", "第一次可能已经成功但响应丢失，再执行会重复扣款、发信或写数据。"),
    q("幂等键解决什么问题？", "让同一逻辑操作的重复请求被识别并复用已有结果，避免副作用重复发生。"),
    q("Agent 如何断点恢复？", "持久化 step、上下文、调用 ID、执行状态、结果与预算，从安全 checkpoint 判断继续、查询或补偿。"),
  ],
  13: [
    q("为什么最小权限对 Agent 特别重要？", "模型可能误判或受注入影响，权限越大，单次错误的影响范围越大。"),
    q("工具风险如何分级？", "可按只读、可逆写、高影响不可逆操作分级，并配置自动执行、确认、审批和隔离策略。"),
    q("什么是间接 Prompt Injection？", "攻击指令隐藏在网页、邮件或工具结果中，诱导 Agent 泄露数据或调用危险能力。"),
    q("沙箱主要限制什么？", "限制文件、网络、进程、系统调用、资源和凭证范围，降低不可信代码或工具的破坏面。"),
  ],
  14: [
    q("Agent 什么时候应该向用户澄清？", "目标或关键参数缺失，且擅自假设会明显影响结果或产生风险时。"),
    q("确认和审批有什么区别？", "确认通常由当前用户确认具体动作；审批还涉及组织权限、责任链和审计。"),
    q("HITL 中断如何恢复？", "持久化待决动作、参数、风险和状态，收到响应后凭恢复标识从 checkpoint 继续。"),
    q("如何避免确认疲劳？", "只对高风险动作确认，合并低风险操作，并清楚展示变化、范围和后果。"),
  ],
  15: [
    q("Agent Eval 的基本组成是什么？", "任务集、成功判据、固定运行协议、最终结果与轨迹采集、指标、失败归因和版本记录。"),
    q("能力评测和回归评测有何区别？", "能力评测探索上限和边界；回归评测用稳定样本阻止已知能力退化。"),
    q("为什么人工看几个 Demo 不够？", "样本少、选择偏差大且无法复现，不能衡量随机性、失败分布和版本变化。"),
    q("为什么评测要记录工具和环境版本？", "Agent 结果由模型、Prompt、工具和环境共同决定，缺版本无法复现或归因。"),
  ],
  16: [
    q("Outcome Eval 与 Trajectory Eval 有何区别？", "前者检查最终结果或环境状态，后者检查中间工具选择、参数、顺序、冗余和违规行为。"),
    q("评测 Agent 为什么 exact match 不够？", "开放回答可能语义相同，外部任务也应检查真实环境状态，而非最后文本是否逐字一致。"),
    q("存在多条正确轨迹时怎么评分？", "定义最终状态、必要约束和成本边界，不把唯一调用序列当作金标准。"),
    q("什么时候轨迹本身必须严格检查？", "当调用顺序影响安全、权限、不可逆副作用或合规要求时。"),
  ],
  17: [
    q("success rate 如何计算？", "成功任务数除以总任务数，但要同时说明样本集、重复次数、随机参数和置信区间。"),
    q("pass@k 表示什么？", "同一任务尝试 k 次时至少一次成功的比例，反映通过多次探索得到成功的能力。"),
    q("pass^k 表示什么？", "同一任务连续 k 次都成功的比例，更强调稳定性和可复现性。"),
    q("Agent 优化为什么要看每成功任务成本？", "单次调用便宜可能导致更多失败和重试，按成功任务统计更接近真实交付成本。"),
  ],
  18: [
    q("LLM-as-a-Judge 适合什么场景？", "适合规则难覆盖的开放答案质量判断，如相关性、完整性和风格；可执行任务应优先客观验证器。"),
    q("LLM Judge 有哪些偏差？", "位置、长度、自我偏好、措辞敏感、标准漂移和被待评文本注入等。"),
    q("如何校准 Judge？", "用人工双标的代表性样本计算一致率和分类型误差，迭代 rubric，并保留固定校准集。"),
    q("如何缓解位置偏差？", "交换候选顺序重复评判或随机化顺序，再检查结论是否稳定。"),
  ],
  19: [
    q("Agent 失败可以分成哪些类？", "目标理解、规划、工具发现、参数、执行、观察理解、上下文、终止、安全和环境等。"),
    q("如何找到失败的根因？", "沿 Trace 找第一个偏离预期的决策点，区分最初错误与后续连锁症状，并用反事实修正验证。"),
    q("重复工具调用一定是规划问题吗？", "不一定，也可能是结果没有结构化、状态未更新、调用失败未标记或终止条件缺失。"),
    q("为什么失败分类必须可行动？", "分类应能指向具体证据、负责人和修复策略，否则只是在给失败改名字。"),
  ],
  20: [
    q("离线评测、Shadow 和 A/B 的区别是什么？", "离线在固定数据运行；Shadow 用真实流量旁路但不影响用户；A/B 将不同版本真实返回给不同用户。"),
    q("Agent 发布前应有哪些门禁？", "确定性测试、核心回归、随机重复评测，以及质量、安全、成本和延迟阈值。"),
    q("什么是评测集过拟合？", "反复针对固定测试样本调 Prompt 或策略，使分数上涨但对新任务泛化不变。"),
    q("线上失败如何进入研发闭环？", "脱敏后复现，标注根因，加入回归集，修复后做新旧版本对照并持续监控。"),
  ],
  21: [
    q("Coding Agent 的执行闭环是什么？", "理解 issue、检索仓库、编辑补丁、运行测试、根据反馈迭代并提交可验证结果。"),
    q("SWE-bench 主要如何判断任务成功？", "在固定仓库版本和环境应用补丁，检查目标失败测试是否转绿，同时原通过测试不应回归。"),
    q("为什么 Coding Agent 需要沙箱？", "仓库和命令都可能不可信，必须限制文件、网络、进程、凭证和资源使用。"),
    q("只看最终测试通过够吗？", "不够，还应检查修改范围、pass-to-pass 回归、作弊或泄漏、成本、时间和轨迹违规。"),
  ],
  22: [
    q("Agent Trace 应记录哪些内容？", "任务和 step id、模型/Prompt/工具版本、输入输出摘要、工具参数与结果、状态变化、耗时、成本和错误。"),
    q("日志、指标、Trace 如何分工？", "指标发现趋势，Trace 还原单任务时序，日志提供具体事件和诊断上下文。"),
    q("如何检测 Agent 没有进展？", "监控重复调用、相似观察、状态变化量、连续错误和预算消耗。"),
    q("Tracing 为什么要做脱敏？", "上下文和工具结果可能含凭证、隐私和业务数据，观测系统本身也可能成为泄露面。"),
  ],
  23: [
    q("长任务为什么应使用 Job 模型？", "任务不应依赖单个 HTTP 连接或进程存活，Job id 支持异步查询、取消、恢复和通知。"),
    q("Event Log 和 Snapshot 有什么区别？", "Event Log 保存状态变化历史便于审计重放；Snapshot 保存某时刻完整状态便于快速恢复。"),
    q("取消 Agent 任务需要做什么？", "传播取消信号、停止新步骤、尽力终止运行调用，并对已发生副作用执行补偿或转人工。"),
    q("为什么恢复时不能直接重放全部工具？", "工具可能已有副作用，重放会重复执行；应查询执行记录并使用幂等键。"),
  ],
  24: [
    q("为什么入口 QPS 不能代表 Agent 实际负载？", "一个用户请求可能放大成多次模型、检索和工具调用，内部流量远大于入口流量。"),
    q("Agent 应设置哪些预算？", "步数、总时间、token、金额、工具次数、并发和资源使用等。"),
    q("背压是什么？", "下游变慢时限制接纳、排队或并发，把容量不足信号传回上游，防止无限堆积。"),
    q("熔断器如何工作？", "连续失败达到阈值后暂时拒绝调用，过一段时间进入半开状态用少量请求探测恢复。"),
  ],
  25: [
    q("模型路由有什么价值？", "按任务难度和风险选择不同模型，在满足质量约束下减少成本和延迟。"),
    q("模型路由本身如何评测？", "看错误路由率、最终成功率、升级比例、延迟和每成功任务成本，而不只是分类准确率。"),
    q("Prompt 变短就一定更好吗？", "不一定。应在固定回归集比较成功率、工具选择、参数正确率、延迟和 token 成本。"),
    q("缓存 Agent 响应有什么风险？", "用户权限、环境状态和时间可能变化，错误命中会返回过时或越权结果，需要明确缓存键和有效期。"),
  ],
  26: [
    q("什么时候应该使用多 Agent？", "存在明确专业分工、可并行探索或独立交叉验证，并且收益能覆盖通信和成本时。"),
    q("Supervisor 模式是什么？", "由中心 Agent 或确定性控制器分配子任务、聚合结果、处理冲突并决定终止。"),
    q("多 Agent 的主要问题是什么？", "调用成本、通信噪声、状态一致性、权限扩散、冲突和失败归因更复杂。"),
    q("Agent 之间应共享全部消息吗？", "通常不应。应按最小必要原则传递结构化任务、结果和产出物引用。"),
  ],
  27: [
    q("Generator-Verifier 架构是什么？", "生成器产生候选，验证器用测试、规则或模型评分，系统据此选择、修正或继续搜索。"),
    q("Best-of-N 的优缺点是什么？", "实现简单且可提升成功上限，但成本近似随 N 增长，效果受候选多样性和 Verifier 准确性限制。"),
    q("搜索为什么在 Agent 中昂贵？", "每个分支都可能需要模型调用、工具执行、环境快照和评分，分支数会迅速增长。"),
    q("什么时候才需要微调 Agent 策略？", "有稳定评测、足够高质量轨迹，并确认错误来自模型策略而非工具、上下文或编排时。"),
  ],
  28: [
    q("Agent 的版本应包含什么？", "模型和参数、Prompt、工具 Schema/实现、编排策略、记忆检索配置、依赖和评测集版本。"),
    q("Agent 如何灰度发布？", "离线回归后依次 Shadow、内部、小流量 Canary，再按护栏指标逐步扩大。"),
    q("工具 Schema 如何做兼容演进？", "新增可选字段较安全；删除、改名或语义变化应版本化，并考虑长任务和旧状态恢复。"),
    q("Agent 回滚为什么可能比普通服务复杂？", "任务状态可能由新版本创建，旧版本未必能解释；还涉及外部副作用和 Schema 兼容。"),
  ],
  29: [
    q("Agent 系统设计题先问什么？", "先问用户和目标、任务规模、工具与副作用、成功标准、延迟成本 SLO、安全和部署约束。"),
    q("设计 Agent 时必须主动覆盖哪些非功能需求？", "终止、超时重试、幂等、持久化、权限、沙箱、观测、评测、成本和回滚。"),
    q("桌面 Agent 的特殊风险是什么？", "可访问本地文件、账号和 GUI，页面内容还可能注入指令，因此权限范围和确认机制非常关键。"),
    q("如何回答框架选型？", "从需求和约束出发比较控制流、状态、恢复、安全、生态和维护成本，而不是只说熟悉哪个框架。"),
  ],
  30: [
    q("如何用两分钟介绍 Agent Harness？", "先定义它是模型外的执行与控制层，再沿模型决策、工具执行、状态循环讲主链路，最后补可靠性、安全、Tracing 和 Eval。"),
    q("面试八股怎样回答更完整？", "先给一句结论，再讲机制，然后说明取舍或失败边界，最后说如何用指标、Trace 或实验验证。"),
    q("Agent Harness 最核心的六条主线是什么？", "执行循环、工具协议、状态上下文、可靠性、安全权限、可观测与评测。"),
    q("后端背景转 Agent Harness 的优势是什么？", "已具备服务、状态、并发、存储和排障能力，只需补模型行为、工具协议、上下文和评测。"),
  ],
};

const requiredQuestions = {
  1: q("Agent Harness、Agent Runtime 和模型 API 分别是什么？", "模型 API 完成一次推理；Agent Runtime 驱动模型、工具和状态循环；Harness 是更完整的运行外壳，还负责模型适配、权限、安全、恢复、Tracing 与评测。"),
  2: q("一次 Function Calling 从模型到工具结果回填如何完成？", "Harness 把工具 Schema 发给模型；模型返回 tool call；Harness 解析、Schema 与业务校验、权限检查后执行工具；再用 tool_call_id 回填结果，模型继续决策或结束。"),
  3: q("MCP 解决什么问题，它与 Agent Harness 是什么关系？", "MCP 标准化应用发现和调用外部工具、资源的方式；Harness 可作为 MCP Host/Client 接入能力，再把工具暴露给模型。MCP 不负责完整 Agent 循环和评测。"),
  4: q("生产级 Agent Loop 需要哪些终止和保护条件？", "除 final answer 外，还要有任务成功判定、max steps、时间/token/费用预算、重复和无进展检测、用户取消、不可恢复错误与人工升级。"),
  5: q("为什么消息历史不能等同于 Agent 状态？", "消息是给模型推理的序列；预算、权限、工具执行状态、checkpoint、重试和 trace 等控制状态需要结构化持久化，不能只靠模型记忆。"),
  6: q("ReAct、Plan-and-Execute 和 Verifier 分别解决什么？", "ReAct 利用逐步观察动态决策；Plan-and-Execute 为长任务建立依赖和全局结构；Verifier 用外部标准检查结果。三者可组合。"),
  7: q("如何判断一个 Agent 框架是否适合生产？", "看控制流是否可控、状态能否持久化恢复、工具和模型是否易扩展、是否支持权限、HITL、Tracing、评测、并发和稳定部署。"),
  8: q("Agent 规划失败时如何恢复？", "先根据 Trace 判断是目标、信息、工具还是局部执行问题；优先局部重试或改计划，连续无进展再全局重规划或转人工。"),
  9: q("如何提高工具选择与参数生成准确率？", "减少和检索候选工具，优化互斥描述与 Schema，增加业务校验和错误反馈，并在固定数据集分别评测工具召回、选择和参数准确率。"),
  10: q("Agent 的短期记忆和长期记忆如何设计？", "短期保存当前任务上下文和工作状态；长期保存经验证的事实或历史经验。长期写入需来源、去重、权限、时效和更新删除机制。"),
  11: q("上下文窗口不足时有哪些压缩方法和风险？", "可截断、摘要、检索和提取结构化状态；风险分别是直接丢失、摘要失真、漏召回和维护成本。应保留原始 Trace 并按当前决策取回。"),
  12: q("Agent 中重试、幂等和 checkpoint 如何配合？", "先按错误类型决定是否重试；副作用调用使用幂等键；在执行前后持久化 checkpoint，恢复时查执行记录决定继续、查询结果或补偿。"),
  13: q("如何防止 Agent 因 Prompt Injection 执行危险操作？", "外部内容视为不可信数据，采用最小权限、工具风险分级、沙箱和数据流隔离；高风险动作由独立代码策略校验并要求明确审批。"),
  14: q("什么时候 Agent 应该 Human-in-the-Loop？", "缺少关键目标、证据冲突、权限不足、风险高或自动恢复失败时；暂停状态要持久化，并支持审批后从 checkpoint 恢复。"),
  15: q("如何从零设计一个 Agent 评测集？", "先定义真实任务分布和成功标准，按类型、难度和风险采样，准备环境与验证器，固定版本重复运行，记录 Outcome、Trajectory、成本延迟并做失败归因。"),
  16: q("为什么 Agent 要同时做 Outcome Eval 和 Trajectory Eval？", "Outcome 确认用户目标是否完成，Trajectory 暴露过程中的误调用、冗余和风险。只看其中一个会漏掉偶然正确或过程合理但结果失败的问题。"),
  17: q("Agent 评测中的 pass@k 与 pass^k 有什么区别？", "pass@k 看 k 次中至少一次成功，反映探索上限；pass^k 看连续 k 次都成功，反映稳定性。二者都要报告采样和重复口径。"),
  18: q("LLM-as-a-Judge 有哪些风险，如何降低？", "它有位置、长度、自我偏好、标准漂移和注入风险；通过清晰 rubric、交换顺序、多次评判、人工校准和优先客观验证降低。"),
  19: q("Agent 失败如何做根因分类？", "定义可行动 taxonomy，沿 Trace 找首个错误决策点，区分根因与连锁症状，再通过修正该点的对照运行验证归因。"),
  20: q("Agent 版本发布前如何做评测门禁？", "先单测和离线回归，再随机重复、Shadow/Canary；对成功率、安全、成本和延迟设阈值，固定模型、Prompt、工具和环境版本并支持回滚。"),
  21: q("Coding Agent 和普通对话 Agent 的 Harness 有何不同？", "它还需管理仓库工作区、文件编辑、Shell 沙箱、长命令、测试验证、补丁 diff 和环境复现，结果更适合由测试和代码状态判断。"),
  22: q("如何用 Trace 定位一次 Agent 失败？", "先从结果回溯每个模型与工具 span，找到最早异常决策，检查当时上下文、版本、参数、工具结果和状态变化，再用回放或对照实验验证。"),
  23: q("长时间 Agent 任务如何支持断点恢复？", "任务与 HTTP 连接解耦，使用 job id、持久化状态机、event log/snapshot 和 checkpoint；工具调用必须有幂等记录，并支持取消与补偿。"),
  24: q("Agent 服务如何做容量和成本控制？", "分别限制任务、租户和工具并发，设置步数、token、时间和费用预算，对下游做背压、队列上限、限流和熔断，并按单成功任务统计成本。"),
  25: q("如何优化 Agent 的质量、延迟和成本？", "先用失败分类确定瓶颈，再选择模型路由、减少工具候选、上下文压缩、缓存或并行；在固定 Eval 上同时检查成功率、P95 和每成功任务成本。"),
  26: q("多 Agent 一定比单 Agent 强吗？", "不一定。只有明确分工、并行探索或独立验证的收益超过通信、状态同步、成本和排障复杂度时才值得使用。"),
  27: q("Verifier 和搜索如何提高 Agent 成功率？", "生成多个候选或展开动作分支，再用测试、规则或模型评分选择和修正；上限取决于候选覆盖和 Verifier 与真实目标的一致性。"),
  28: q("Agent 系统为什么要联合版本化？", "行为由模型、Prompt、工具 Schema/实现、编排、记忆和环境共同决定，单独版本化代码无法复现、归因或安全回滚。"),
  29: q("设计一个生产级 Agent 平台时你会拆哪些模块？", "API/任务层、模型网关、Runtime/状态机、工具注册与执行、上下文记忆、权限沙箱、任务队列与持久化、Tracing、Eval 和发布治理。"),
  30: q("你如何定义 Agent Harness 方向的核心能力？", "能把模型决策接到真实工具和状态上，并通过可靠性、安全、可观测和评测机制，让系统在明确成本和风险边界内稳定完成任务。"),
};

export function questionsForDay(day) {
  return [
    { ...requiredQuestions[day.day], required: true },
    ...(extraQuestions[day.day] || []),
  ];
}
