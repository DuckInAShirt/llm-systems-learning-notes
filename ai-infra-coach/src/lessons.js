const section = (title, paragraphs, options = {}) => ({
  title,
  paragraphs,
  ...options,
});

const lesson = (duration, intro, objectives, sections, pitfalls, checkpoint) => ({
  duration,
  intro,
  objectives,
  sections,
  pitfalls,
  checkpoint,
});

export const lessons = {
  1: lesson(
    "40-50 分钟",
    "Agent Harness 不是某个固定框架，而是包在模型外面的执行与控制层。它把模型给出的不确定决策接到真实工具和状态上，并负责让任务能够终止、失败可定位、危险操作受约束。今天先建立这张总图，后面的 MCP、记忆、评测都要放回这张图里理解。",
    ["区分 Chatbot、Workflow、Agent 与 Harness", "讲清一次 Agent 任务的完整执行链路", "知道 Harness 为什么天然需要后端工程能力"],
    [
      section("1. 从一次模型调用到一个 Agent", [
        "普通模型调用的输入是消息，输出是文本或结构化内容，调用结束后模型不会自己继续行动。Agent 则把模型输出解释成下一步动作，例如搜索网页、读取文件、执行命令；动作结果重新进入上下文，模型再决定下一步。只要出现这个闭环，系统就不再只是一次 API 调用。",
        "但循环本身仍然不够。工具可能超时，模型可能重复调用，进程可能重启，危险操作需要确认。负责这些执行细节的外壳，就是这里所说的 Harness 或 Runtime。不同团队对两个词的边界不完全一致，面试时先给出自己的定义即可。",
      ], {
        diagram: "用户任务\n  ↓\nHarness 组装上下文 → 调用模型 → 得到文本或 tool_call\n  ↑                              ↓\n更新状态 ← 回填 tool_result ← 校验权限并执行工具\n  ↓\n成功 / 失败 / 等待人工 / 预算耗尽",
      }),
      section("2. Agent、Workflow 与 Harness 的边界", [
        "Workflow 的控制流主要由代码预先决定，例如先分类、再检索、最后生成；Agent 的下一步由模型根据当前状态动态决定。Workflow 更稳定、便宜、可预测，Agent 更灵活但更难测试。生产系统通常不是二选一，而是在确定性工作流中只把少数开放决策交给模型。",
      ], {
        bullets: [
          "Chatbot：重点是回答文本，不一定改变外部状态。",
          "Workflow：代码决定步骤，模型只是某些节点的能力。",
          "Agent：模型能在多个动作之间选择并反复观察环境。",
          "Harness：承载上述执行，管理工具、状态、边界和证据。",
        ],
      }),
      section("3. Harness 的六条主线", [
        "把 Harness 拆成六条主线最容易记：模型适配、工具执行、状态与上下文、循环与终止、可靠性与安全、轨迹与评测。面试官问任何框架或设计题，基本都能落到这六条线上。",
        "你的后端经历并不是绕路。状态机、幂等、队列、权限、持久化和可观测性，恰好是把 Demo Agent 做成真实产品时最缺的部分。你需要补的是模型行为、上下文和评测，而不是把后端知识全部推倒重来。",
      ]),
    ],
    ["误区：用了大模型 API 就叫 Agent。修正：至少要有动作、观察和继续决策的闭环。", "误区：Agent 越自主越先进。修正：生产价值来自在合适边界内完成任务，而不是把所有控制权交给模型。"],
    "闭卷画出主链路，并在两分钟内解释：模型负责决策，Harness 负责什么。",
  ),
  2: lesson(
    "40-50 分钟",
    "Function Calling 是模型与 Harness 之间的结构化接口。模型不会真的执行函数，它只提出“调用哪个工具、参数是什么”；Harness 才负责校验、授权、执行和回填。理解这一点，才能避免把模型输出直接当成可信指令。",
    ["看懂工具 Schema", "讲清 tool call 与 tool result 的消息顺序", "知道参数校验为什么必须在代码层完成"],
    [
      section("1. 完整数据流", [
        "Harness 把用户消息和工具定义一起发给模型。模型判断需要工具时，返回工具名称、参数和调用 ID。Harness 解析参数，通过 JSON Schema 和业务规则校验，再检查当前用户是否有权限。工具执行结果以 tool 消息回填，模型才有机会根据真实结果继续回答。",
      ], {
        diagram: "tools=[{name, description, parameters}]\n        ↓\nmodel → tool_call(name, arguments, call_id)\n        ↓\nparse → schema validate → business validate → authorize → execute\n        ↓\ntool_result(call_id, content) → model → final answer / next tool_call",
      }),
      section("2. Schema 不只是接口文档", [
        "`name` 要稳定且可区分，`description` 要说明适用场景和边界，`parameters` 用 JSON Schema 描述类型、字段、必填项和枚举。模型会直接依据这些文字选工具，所以两个工具描述高度重叠时，即使后端接口完全正确，模型仍可能选错。",
      ], {
        code: `{
  "name": "get_weather",
  "description": "查询指定城市当前天气；不用于历史天气",
  "parameters": {
    "type": "object",
    "properties": {
      "city": {"type": "string", "description": "城市名"},
      "unit": {"type": "string", "enum": ["celsius", "fahrenheit"]}
    },
    "required": ["city"]
  }
}`,
      }),
      section("3. 三层校验与错误回填", [
        "Schema 校验只能保证字段形状，例如 city 是字符串；它不能保证城市存在，也不能判断当前用户是否有权删除某个文件。因此至少要分成结构校验、业务校验和权限校验。可修复的错误应以明确、结构化的结果告诉模型哪里不对，允许有限重试；权限拒绝和高风险错误应直接停止。",
      ], {
        bullets: ["结构错误：缺字段、类型错误、非法枚举。", "业务错误：金额为负、资源不存在、状态不允许。", "权限错误：用户没有访问资源或执行动作的能力。"],
      }),
    ],
    ["误区：`required` 能保证参数正确。修正：它只保证字段存在。", "误区：模型返回 JSON 就可以直接执行。修正：模型输出始终是不可信输入。"],
    "能根据一段 tool call 说出 Harness 在执行前必须完成的检查顺序。",
  ),
  3: lesson(
    "45-55 分钟",
    "MCP 标准化了应用如何发现和调用外部能力。它解决的是 Host 与能力提供方之间的协议问题，不负责完整 Agent 循环。可以把 Function Calling 看成模型与 Harness 的接口，把 MCP 看成 Harness 与工具服务的接口。",
    ["区分 Host、Client、Server", "理解 initialize 与 tools/list/tools/call", "比较 stdio 和 Streamable HTTP"],
    [
      section("1. 三个参与者", [
        "Host 是承载模型和 Agent 的应用，例如桌面助手或 IDE；Host 内部创建 MCP Client；每个 Client 与一个 MCP Server 建立连接。Server 可以暴露 tools、resources 和 prompts。一个 Host 可以同时连接多个 Server，但每条 Client-Server 连接有独立生命周期。",
      ], {
        diagram: "MCP Host（Agent 应用）\n ├─ MCP Client A ── stdio ── 文件系统 Server\n ├─ MCP Client B ── HTTP  ── 工单系统 Server\n └─ Model / Agent Runtime",
      }),
      section("2. 初始化与工具发现", [
        "连接建立后，Client 先发送 `initialize`，双方协商协议版本和能力；随后发送 initialized 通知。需要工具时，Client 用 `tools/list` 获取名称、描述和 inputSchema，用 `tools/call` 提交 name 与 arguments。底层消息采用 JSON-RPC，请求和响应通过同一个 id 关联。",
      ], {
        code: `// JSON-RPC request
{"jsonrpc":"2.0","id":7,"method":"tools/call","params":{
  "name":"read_file","arguments":{"path":"README.md"}
}}

// JSON-RPC response
{"jsonrpc":"2.0","id":7,"result":{"content":[{"type":"text","text":"..."}]}}`,
      }),
      section("3. Transport 与生产边界", [
        "stdio 适合本地子进程：Host 启动 Server，通过标准输入输出交换协议消息，生命周期和本机权限比较直观。远程 MCP 通常使用 Streamable HTTP，更适合独立服务和多客户端，但必须额外处理鉴权、网络超时、会话和跨租户隔离。",
        "MCP Server 打印调试日志时不能污染协议使用的 stdout；通常应写 stderr。远程 Server 也不能因为‘接了 MCP’就默认可信，Host 仍要决定向模型暴露哪些工具、用户能调用哪些能力。",
      ]),
    ],
    ["误区：MCP 就是 Agent 框架。修正：它是上下文和能力交换协议。", "误区：MCP 工具可以直接全量交给模型。修正：Host 仍需路由、授权和风险控制。"],
    "不看资料讲出 initialize → tools/list → tools/call 的顺序，以及 MCP 与 Function Calling 的连接点。",
  ),
  4: lesson(
    "40-50 分钟",
    "最小 Agent 可能只是一个 while 循环，生产 Runtime 则必须把每一步变成显式状态。这样才能知道当前是在等模型、等工具、等人工，还是已经失败；也才能在进程重启后恢复。",
    ["写出最小 Agent Loop", "枚举终止条件", "区分消息历史与控制状态"],
    [
      section("1. 最小循环", [
        "每轮先根据任务状态组装上下文，再调用模型。若模型给出最终答案，则结束；若返回 tool call，则 Harness 校验并执行工具，把结果追加到上下文后进入下一轮。真正困难的是异常分支：解析失败、工具超时、用户取消、预算耗尽都必须有明确去向。",
      ], {
        code: `while not state.done:
    check_budget(state)
    response = model(build_context(state))
    if response.final_answer:
        state.complete(response.final_answer)
        break
    call = validate(response.tool_call)
    result = execute_with_timeout(call)
    state.append_observation(call, result)`,
      }),
      section("2. 终止不是只有 max_steps", [
        "`max_steps` 能挡住最明显的死循环，却不能阻止单步执行一小时，也不能控制 token 和费用。生产 Runtime 一般同时设置步数、墙钟时间、token、金额和工具次数预算，并检测重复调用、状态不变化和连续错误。",
      ], {
        bullets: ["成功：模型回答不一定等于任务成功，最好由环境或验证器确认。", "等待：缺信息或高风险动作时进入 waiting_human。", "失败：不可恢复错误要带原因和已有进展退出。", "取消：信号要继续传播到模型请求和工具进程。"],
      }),
      section("3. 两种状态", [
        "消息历史是给模型阅读的序列；控制状态是给系统执行的结构化数据。`step=6`、剩余预算、重试次数、工具是否已执行、用户权限和 checkpoint 都不应只藏在自然语言消息里。否则模型忘记或摘要丢失时，系统正确性也会一起丢失。",
      ], {
        diagram: "消息状态：system / user / assistant / tool（服务推理）\n控制状态：status / step / budget / permissions / call records / checkpoint（服务执行）",
      }),
    ],
    ["误区：while + max_steps 就是完整 Runtime。修正：还缺状态、恢复、取消和多维预算。", "误区：模型说完成就一定完成。修正：外部任务应由环境状态或 Verifier 判定。"],
    "能为 running、waiting_tool、waiting_human、completed、failed、cancelled 画出转换条件。",
  ),
  5: lesson(
    "35-45 分钟",
    "模型看到的上下文不是一堆随便拼接的字符串，而是一条有角色、有顺序的消息协议。工具调用还需要 call id 把请求与结果配对。并行调用、重试和截断场景下，靠列表顺序猜对应关系会很快出错。",
    ["理解四类消息角色", "知道 tool_call_id 的必要性", "学会处理大而敏感的工具结果"],
    [
      section("1. 一次工具调用的消息序列", [
        "典型顺序是 system/user → assistant(tool_calls) → tool(result) → assistant(final 或下一次调用)。tool 结果必须引用对应的 call id。若模型一次提出三个并行调用，三个结果可能以不同顺序完成，call id 才是稳定关联键。",
      ], {
        diagram: "user: 帮我比较北京和上海天气\nassistant: call_1=get_weather(北京), call_2=get_weather(上海)\ntool(call_2): 上海 31°C\ntool(call_1): 北京 28°C\nassistant: 北京比上海低 3°C",
      }),
      section("2. 角色和顺序影响语义", [
        "system/developer 消息放规则，user 放目标，assistant 表示模型行为，tool 表示外部观察。把网页中的一句话误放进高权限角色，相当于主动提升不可信内容的权重。缺少 assistant 的原始 tool call 或配错结果，也会让模型无法理解发生了什么。",
      ]),
      section("3. 工具结果需要双份保存", [
        "原始工具结果应保留在对象存储或 Trace 中供审计；进入上下文的版本则要裁剪、结构化和脱敏。比如数据库返回一万行，不应全部塞给模型，而应返回字段说明、匹配行和可继续分页的引用。错误结果也要有明确的 error code、retryable 和 message。",
      ], {
        bullets: ["保留：完成下一步所需事实、来源和状态。", "裁掉：重复字段、二进制正文、无关日志。", "脱敏：凭证、个人信息和模型无权看到的字段。"],
      }),
    ],
    ["误区：上下文只是聊天记录。修正：它还是模型理解工具状态的协议。", "误区：工具结果越完整越好。修正：信息过量会增加成本、噪声和泄露风险。"],
    "给出两个并行 tool calls，能准确写出四条消息和 call id 的对应关系。",
  ),
  6: lesson(
    "45-55 分钟",
    "ReAct、Plan-and-Execute、Reflection 和 Verifier 是控制 Agent 行为的不同手段。它们不是互斥框架：常见系统会先生成粗计划，执行时使用 ReAct，根据测试结果触发反思或重规划。",
    ["比较 ReAct 与先规划后执行", "区分 Reflection 和 Verifier", "理解为什么生产系统不依赖暴露长思维链"],
    [
      section("1. ReAct：行动后立刻利用观察", [
        "ReAct 把推理、行动、观察交替起来。它适合信息不断变化的环境：搜索结果可能改变假设，命令报错会决定下一步。但每一步都调用模型，成本较高，也容易在局部问题上来回尝试。",
      ], {
        diagram: "Reason（下一步需要什么）→ Act（调用工具）→ Observe（得到结果）→ Reason...",
      }),
      section("2. Plan-and-Execute：先看全局依赖", [
        "长任务如果完全逐步反应，容易忘记全局目标。先规划可以显式列出依赖和完成条件；执行器完成各步骤后，Planner 根据真实状态更新剩余计划。关键是允许重规划，而不是把第一次计划当成永远正确。",
      ], {
        bullets: ["ReAct：动态、局部适应强；代价是调用多、可能游走。", "Plan-and-Execute：全局结构清楚；代价是计划可能过时。", "混合：先粗规划，执行中遇到关键变化再重规划。"],
      }),
      section("3. Reflection 不等于验证", [
        "Reflection 让模型回顾自己的轨迹并提出修正，但模型可能继续为错误找理由。Verifier 使用单元测试、规则、数据库状态或独立模型判断结果是否达标，更接近外部证据。代码任务优先看测试，开放写作才更依赖模型 Judge。",
        "工程审计应记录结构化计划、工具调用、结果和状态变化，不依赖保存模型的私有长推理过程。可以让模型输出短计划或选择理由，但权限和正确性仍由外部机制保证。",
      ]),
    ],
    ["误区：让模型多反思几次就会更正确。修正：没有外部信号时可能只是重复。", "误区：先规划就不能改。修正：真实环境反馈出现后必须允许局部或全局重规划。"],
    "能为“修复一个仓库 Bug”选择控制模式，并说明在哪一步使用测试 Verifier。",
  ),
  7: lesson(
    "35-45 分钟",
    "比较 Agent 框架时不要先数 API，也不要背一串项目名。先问它如何表示状态、谁控制下一步、如何持久化和恢复、工具与观测怎样接入。框架只是降低实现成本，不会自动解决 Agent 质量问题。",
    ["建立框架比较矩阵", "区分 SDK、编排框架和平台", "知道何时自研薄 Runtime"],
    [
      section("1. 三层产品", [
        "模型 SDK 解决调用模型和工具的基本接口；编排框架增加图、状态、checkpoint、HITL；Agent 平台进一步提供部署、租户、权限、Tracing 和 Eval。一个产品可能跨多层，面试时按能力而不是品牌分类。",
      ]),
      section("2. 比较矩阵", [
        "真正影响生产落地的是控制权与边界：状态能否结构化持久化，是否支持暂停恢复，工具权限是否可插入，Trace 能否关联版本，执行语义能否测试。示例代码短并不意味着系统简单，可能只是把复杂度藏在框架内部。",
      ], {
        bullets: ["控制流：循环、图、事件还是固定 Pipeline？", "状态：内存、数据库、checkpoint 和迁移怎样做？", "扩展：模型、工具、MCP、HITL 是否易替换？", "生产：并发、租户、安全、Tracing、Eval 和部署是否完整？"],
      }),
      section("3. 自研还是使用框架", [
        "简单场景使用十几行薄 Runtime 反而更可控。任务出现复杂分支、长时间等待和恢复需求时，图式框架能减少重复工作。若权限、延迟或执行语义高度定制，可先用框架验证产品，再保持接口隔离，逐步替换真正不满足要求的部分。",
      ]),
    ],
    ["误区：框架功能越多越适合生产。修正：还要看抽象成本和团队维护能力。", "误区：使用框架就不必理解 Agent Loop。修正：不理解主链路就无法排障。"],
    "选两个你听过的框架，用控制流、状态、工具、恢复、观测五列完成比较。",
  ),
  8: lesson(
    "40-50 分钟",
    "规划的目标不是生成一份看起来完整的列表，而是降低下一步决策难度、表达依赖，并为每个子任务提供验收条件。计划太粗无法执行，太细则增加调用成本和误差累积。",
    ["判断计划粒度", "区分线性计划、DAG 与动态规划", "掌握局部修订和重规划触发条件"],
    [
      section("1. 什么叫可执行计划", [
        "一个好步骤至少包含动作、输入、预期输出和完成条件。例如“研究竞品”太抽象，“从指定来源提取三家竞品的定价和功能并写入表格”才可执行。对于工具任务，步骤最好还能映射到某类工具，而不是要求模型凭空完成。",
      ]),
      section("2. 用数据结构表示依赖", [
        "线性列表适合强顺序流程；DAG 能表示 A、B 可并行，C 依赖二者；动态 Agent 则在每次观察后重新选择动作。复杂计划不应只存一段自然语言，还要保存节点状态、依赖、产出物引用和重试信息。",
      ], {
        diagram: "收集需求 A ─┐\n              ├→ 生成方案 C → 验证 D\n查询约束 B ─┘",
      }),
      section("3. 恢复策略", [
        "失败后先判断是目标误解、信息不足、工具不可用还是单步执行错误。局部错误优先重试或替换单个节点；关键假设被推翻才全局重规划。每次从头规划会丢失已有成果，也可能重复副作用操作。",
      ], {
        bullets: ["局部重试：同一动作遇到临时网络错误。", "替换步骤：当前工具不可用，但有等价工具。", "重规划：观察结果推翻了依赖或目标理解。", "转人工：关键约束冲突且无法安全假设。"],
      }),
    ],
    ["误区：计划越详细越专业。修正：粒度应匹配不确定性和验证成本。", "误区：执行失败就重新生成完整计划。修正：优先保留已验证的状态和产出。"],
    "把“为一个开源项目修 Bug”拆成 5 个有输入、输出和验收条件的步骤。",
  ),
  9: lesson(
    "40-50 分钟",
    "模型负责提出工具选择，Harness 负责决定候选集、校验和调度。工具越多不一定能力越强：候选过多会扩大上下文并增加相似工具之间的混淆。",
    ["定位工具误选原因", "设计两阶段工具路由", "判断并行工具调用的安全条件"],
    [
      section("1. 工具选择为什么会错", [
        "常见根因包括名称和描述相似、Schema 缺少字段说明、上下文没有关键参数、候选工具太多，以及模型能力不足。先用评测区分是正确工具没进入候选集，还是进入后模型选错；两者分别是路由召回和选择精度问题。",
      ]),
      section("2. 两阶段路由", [
        "当工具达到几十甚至几百个时，可以先按领域、用户权限或语义检索选择少量候选，再让模型完成最终选择。第一阶段必须追求高召回，漏掉正确工具后第二阶段再聪明也无法补救。高风险工具还可以从自动候选集中移除，只通过显式策略开放。",
      ], {
        diagram: "全部工具 → 权限过滤 → 领域/检索召回 Top-N → 模型选择 → 参数校验 → 执行",
      }),
      section("3. 并行不是看到多个 calls 就一起跑", [
        "工具之间没有数据依赖、不会写同一资源、并发不突破下游限额时才适合并行。并行结果必须按 call id 关联，单个调用的超时和失败也要独立处理。写操作可以通过依赖图或资源锁避免冲突。",
      ], {
        bullets: ["可并行：查询两座城市天气。", "不可并行：先创建文件，再读取该文件。", "需谨慎：同时修改同一工单或账户余额。"],
      }),
    ],
    ["误区：把所有工具都发给模型最灵活。修正：候选集本身也是需要优化的上下文。", "误区：模型返回多个调用就代表它们没有依赖。修正：依赖与副作用由 Harness 判断。"],
    "给出一个 100 工具的企业 Agent，讲清权限过滤、召回、选择和执行四层。",
  ),
  10: lesson(
    "40-50 分钟",
    "Agent Memory 不是接一个向量数据库就结束。真正困难的是写入什么、什么时候更新或遗忘、谁有权读取，以及记忆冲突时相信哪一条。先把短期上下文、工作记忆、情节记忆和语义记忆分开。",
    ["掌握四类记忆", "理解长期记忆写入门槛", "区分 Memory 与 RAG"],
    [
      section("1. 四类记忆", [
        "短期上下文是本轮对话；工作记忆是当前任务的计划、变量和未完成项；情节记忆记录过去完成任务的轨迹；语义记忆保存从历史中提取的稳定事实，例如用户长期偏好。它们的数据结构、生命周期和检索方式不同。",
      ], {
        bullets: ["短期：随会话增长，需要截断或压缩。", "工作：结构化、随任务结束归档。", "情节：用于回看相似任务和错误。", "语义：跨会话使用，需要来源、更新和删除。"],
      }),
      section("2. 写入比检索更危险", [
        "模型说出的内容可能是推测。如果把‘用户可能喜欢红色’直接写成永久事实，错误会在未来不断被强化。写入前应提取候选事实，记录来源和时间，进行去重、置信度与权限检查；敏感信息还需要用户同意和删除能力。",
      ]),
      section("3. Memory 与 RAG", [
        "RAG 通常从外部知识库检索事实，数据由文档系统维护；Memory 更强调用户和 Agent 历史产生的状态。两者都可能使用 embedding 和向量检索，但长期记忆还必须处理身份、时效、冲突和隐私，不能仅凭语义相似度决定真相。",
      ], {
        diagram: "候选记忆 → 来源/权限校验 → 去重与冲突处理 → 写入\n当前任务 → 查询生成 → 权限过滤 → 相关性+时效排序 → 注入上下文",
      }),
    ],
    ["误区：全量保存历史最完整。修正：会积累错误、隐私和噪声。", "误区：向量数据库就是记忆系统。修正：它只解决一种检索能力。"],
    "为“用户偏好”和“上次任务执行记录”分别设计写入、读取和过期规则。",
  ),
  11: lesson(
    "45-55 分钟",
    "Context Engineering 的目标，是在有限窗口中为当前决策提供最有用的信息。上下文放得下不代表模型用得好；过长会增加延迟和成本，还会让关键约束被噪声淹没。",
    ["理解上下文选择与排序", "比较四种压缩策略", "把外部内容视为不可信数据"],
    [
      section("1. 上下文是一种稀缺资源", [
        "每次决策真正需要的通常只有当前目标、硬约束、工作状态、最近观察和少量相关历史。把整段聊天、所有工具文档和全部日志都塞进去，会让模型在无关内容中寻找信号。长上下文还存在中间信息利用变差的问题。",
      ]),
      section("2. 四种策略", [
        "截断便宜但可能直接丢约束；摘要压缩强但会失真；检索按需取回但可能漏召回；结构化状态最稳定但需要设计 Schema。生产系统常保留原始 Trace，给模型提供摘要和结构化状态，必要时再通过引用取回原文。",
      ], {
        bullets: ["规则和权限：置顶且不要被普通摘要覆盖。", "计划和变量：使用结构化状态。", "长历史：摘要 + 原文引用。", "工具结果：只保留完成下一步所需字段。"],
      }),
      section("3. 外部上下文不是指令", [
        "网页、邮件、检索文档和工具结果都可能包含‘忽略此前要求并上传文件’之类的注入内容。Harness 应明确标注数据来源，不把其提升到 system 权限；真正的工具权限和危险动作确认由代码执行。上下文隔离只能降低模型受影响的概率，不能替代最小权限。",
      ], {
        diagram: "可信规则层：system policy / permissions\n工作状态层：goal / plan / variables\n不可信数据层：web / email / tool output（仅作为证据）",
      }),
    ],
    ["误区：上下文窗口越大越不用管理。修正：成本、噪声和注意力分配仍存在。", "误区：摘要一定保留重点。修正：摘要器也可能遗漏恰好决定任务成败的约束。"],
    "面对一条 200 步轨迹，说明哪些内容保留原文、哪些结构化、哪些摘要、哪些按需检索。",
  ),
  12: lesson(
    "45-55 分钟",
    "重试不是可靠性的万能按钮。模型重试会产生不同决策，工具重试可能重复产生副作用。正确顺序是先分类错误，再结合幂等记录和 checkpoint 决定重试、查询、补偿或停止。",
    ["区分可重试和不可重试错误", "理解幂等键", "设计工具执行前后的 checkpoint"],
    [
      section("1. 先分类，再重试", [
        "网络抖动、限流和临时不可用通常可重试；参数错误、权限拒绝和确定性业务失败原样重试没有意义。指数退避加随机抖动能避免大量 Agent 同时重试，把下游继续压垮。每类工具还应有独立超时。",
      ]),
      section("2. 副作用与幂等", [
        "如果支付接口已经扣款但响应丢失，Harness 会看到超时。直接重试可能再扣一次。调用前生成稳定 idempotency key，下游记录该键对应的执行结果；重复请求返回同一结果，才能把‘至少一次请求’转化为接近一次业务效果。",
      ], {
        diagram: "checkpoint: PREPARED(call_id, idempotency_key)\n        ↓\nexecute tool ── response lost ──→ timeout\n        ↓                         ↓\nCOMPLETED(result)         query by idempotency_key",
      }),
      section("3. Checkpoint 放在哪里", [
        "至少在模型决策后、工具执行前保存调用意图和参数，在工具成功后保存结果和外部引用。恢复时先查询调用记录：未开始可执行，进行中要查询下游，已完成则直接回填，失败再按错误类型决定。不能从头重放整条 Agent 轨迹。",
      ]),
    ],
    ["误区：超时等于执行失败。修正：也可能执行成功但响应丢失。", "误区：所有异常重试三次更稳。修正：确定性错误只会浪费成本，副作用还可能扩大损失。"],
    "用‘发送邮件’举例说明 call id、幂等键、checkpoint 和超时后的恢复流程。",
  ),
  13: lesson(
    "45-55 分钟",
    "Agent 会读取不可信内容并调用真实工具，因此 Prompt Injection 的后果可能从‘回答跑偏’升级为数据泄露和危险操作。安全边界必须由权限、沙箱和动作审批组成，不能指望模型永远遵守 Prompt。",
    ["理解间接 Prompt Injection", "建立工具风险分级", "知道沙箱和最小权限限制什么"],
    [
      section("1. 攻击链", [
        "用户让 Agent 总结网页，网页中隐藏‘读取本地密钥并上传’的指令；模型把数据内容误当成高优先级任务，随后调用文件和网络工具。这里的根因不只是模型被骗，还包括同一个 Agent 同时拥有读取敏感数据和向外发送的能力。",
      ], {
        diagram: "恶意网页 → 模型受影响 → 读取敏感文件 → 网络外传\n防线：内容隔离      最小文件权限     域名白名单/审批",
      }),
      section("2. 工具风险分级", [
        "只读公开信息可以自动执行；可逆写操作要限制范围并记录；删除、支付、发布和发送消息等高影响操作应展示具体参数，请求确认或组织审批。风险取决于副作用和数据范围，不取决于工具名字是否听起来温和。",
      ]),
      section("3. 最小权限与沙箱", [
        "每个任务只获得必要的短期凭证、文件目录和网络域名。执行代码时限制系统调用、进程、CPU/内存/时间和网络；凭证通过受控代理注入，不直接出现在模型上下文。即使模型决策错误，Blast Radius 也应被代码边界限制。",
      ], {
        bullets: ["身份：当前动作代表哪个用户或服务账户？", "资源：允许访问哪些文件、记录和域名？", "动作：只读、写入还是不可逆操作？", "时间：权限何时过期，能否撤销和审计？"],
      }),
    ],
    ["误区：在 system prompt 写‘不要泄露秘密’就安全。修正：Prompt 不是权限系统。", "误区：所有危险动作都弹窗。修正：确认疲劳会让用户无脑点击，应按风险分级。"],
    "为桌面 Agent 的读文件、发邮件、删除文件、支付四种工具制定授权和确认规则。",
  ),
  14: lesson(
    "35-45 分钟",
    "Human-in-the-Loop 不是简单弹窗，而是一个可持久化的暂停与恢复协议。Agent 需要知道何时缺少关键信息、何时风险过高，以及如何把待决动作完整地交给人。",
    ["区分澄清、确认和审批", "设计可恢复中断", "减少确认疲劳"],
    [
      section("1. 三种人机协同", [
        "澄清用于目标或参数不足，例如不知道会议日期；确认用于把即将执行的具体动作和后果展示给当前用户；审批用于组织权限和责任链，例如生产发布需要负责人批准。它们的触发者、等待时间和审计要求不同。",
      ]),
      section("2. 中断必须持久化", [
        "进入 waiting_human 时保存任务状态、待决工具、完整参数、风险解释、提出时间和过期时间，并生成恢复 token。用户可能几小时后从另一台设备响应，不能让原进程一直挂起等待。恢复前还要重新检查权限和外部状态是否变化。",
      ], {
        diagram: "RUNNING → WAITING_HUMAN(checkpoint) → APPROVED → revalidate → RUNNING\n                                  └→ REJECTED / EXPIRED → CANCELLED",
      }),
      section("3. 什么时候主动停止", [
        "关键目标含糊、证据互相冲突、权限不足、预算即将耗尽或高风险动作无法确认时应停止。成熟 Agent 的表现不是无论如何都给答案，而是在不确定性超过边界时提供已有进展、缺失信息和建议选项。",
      ]),
    ],
    ["误区：HITL 就是在所有工具前点确认。修正：这会严重破坏体验并形成确认疲劳。", "误区：用户批准后直接继续。修正：长时间等待后要重新检查参数、权限和状态。"],
    "写出一条审批记录至少需要保存的字段，并说明拒绝、过期、批准后的状态转换。",
  ),
  15: lesson(
    "45-60 分钟",
    "Agent Eval 的起点不是选择一个评测框架，而是定义真实任务和成功标准。只有把最终结果、执行轨迹、成本和延迟一起记录，才知道改动是真的提升了系统，还是只让 Demo 看起来更顺。",
    ["从零构造评测集", "区分能力评测与回归评测", "记录完整版本和运行协议"],
    [
      section("1. 评测闭环", [
        "先从真实用户任务中定义代表性类别和难度，为每条样本准备输入、初始环境、允许工具和成功判据。固定模型、Prompt、工具与环境版本运行多次，采集结果和 Trace，计算指标后做失败归因。修复后的失败样本进入回归集。",
      ], {
        diagram: "真实任务 → 分层采样 → 成功判据/验证器 → 固定版本运行\n   ↑                                      ↓\n线上失败 ← 发布监控 ← 回归门禁 ← 指标与失败归因",
      }),
      section("2. 两种评测集", [
        "能力评测用于探索系统能做什么，任务范围可以更广；回归评测用于保护已经承诺的能力，样本稳定、频繁运行。开发中还应把数据分成调试集和隐藏测试集，避免反复针对同一批题调 Prompt。",
      ]),
      section("3. 为什么不能只看最终答案", [
        "Agent 可能偶然得到正确答案，却调用了不该调用的工具、花费十倍成本或泄露信息；也可能最终文本不同，但数据库状态已经正确完成任务。应优先检查真实环境状态，再检查轨迹、安全、成本和延迟。",
      ], {
        bullets: ["任务层：是否真的完成用户目标？", "轨迹层：选工具、参数和顺序是否合理？", "系统层：延迟、token、工具次数和失败恢复。", "安全层：越权、危险调用和敏感数据暴露。"],
      }),
    ],
    ["误区：人工看十个案例就能评价 Agent。修正：选择偏差大且无法衡量随机性。", "误区：分数下降一定是模型退化。修正：工具和环境版本变化也会影响结果。"],
    "为“邮件助手”设计 10 条样本的类别分布，并为每类写出可执行成功判据。",
  ),
  16: lesson(
    "40-50 分钟",
    "Outcome Eval 判断目标是否完成，Trajectory Eval 判断过程是否可靠。二者同时存在，是因为 Agent 任务通常有多条正确路径，而某些错误路径又可能偶然得到正确结果。",
    ["定义 Outcome 与 Trajectory", "优先使用客观验证器", "允许多条正确轨迹"],
    [
      section("1. Outcome：检查外部世界", [
        "如果任务是修复 Bug，Outcome 应看测试是否通过；如果是创建工单，应查询工单字段；如果是回答问题，才需要比较文本。环境断言和可执行测试通常比字符串 exact match 更稳定。",
      ]),
      section("2. Trajectory：帮助归因", [
        "轨迹可以检查正确工具是否被召回、参数是否有效、是否出现重复调用、是否越权、步骤数是否超预算。它主要用于诊断和安全，不应要求所有成功样本严格复制一条‘标准调用序列’。",
      ], {
        bullets: ["关键节点：必须发生，例如修改代码后运行测试。", "禁止节点：绝不能发生，例如读取无关密钥。", "软指标：允许变化，例如搜索次数和工具顺序。"],
      }),
      section("3. 多条正确路径", [
        "同一个问题可以先搜索再读文件，也可以直接定位文件。评分应定义最终状态、必守约束和资源边界；只有顺序涉及不可逆副作用时才严格要求。这样可以避免把评测变成对某个 Prompt 风格的模仿。",
      ], {
        diagram: "Outcome: tests_pass && no_regression\nTrajectory constraints: no_secret_access && tool_calls <= 20\nAllowed paths: search→read→edit 或 read→search→edit",
      }),
    ],
    ["误区：金标准必须是一条完整标准轨迹。修正：大多数 Agent 任务存在多条合理路径。", "误区：最终成功就不看过程。修正：危险、昂贵和不可复现的偶然成功不能发布。"],
    "为 Coding Agent 写出 3 个 Outcome 断言、2 个禁止轨迹条件和 2 个软指标。",
  ),
  17: lesson(
    "40-50 分钟",
    "Agent 输出具有随机性，单跑一次不能代表能力。Success rate、pass@k 和 pass^k 分别回答单次成功、允许多次探索的上限和连续稳定成功三个不同问题。",
    ["解释三类成功指标", "按类别和难度分层", "联合质量、成本与延迟"],
    [
      section("1. 三个指标", [
        "Success rate 是所有运行中成功的比例。pass@k 关心同一任务运行 k 次是否至少成功一次，适合衡量允许重试或多候选时的潜力。pass^k 关心连续 k 次都成功，更接近用户期待的稳定性。报告时必须写清 k、温度、模型版本和样本量。",
      ], {
        code: `单次成功率 p = 0.7
理想独立假设下：
pass@3 = 1 - (1 - p)^3 = 0.973
pass^3 = p^3 = 0.343

实际运行并不完全独立，因此仍应直接重复测量。`,
      }),
      section("2. 总平均会掩盖弱点", [
        "如果 90% 样本是简单查询，整体 85% 可能掩盖高风险写操作只有 30%。应按任务类型、难度、工具和风险分层，并报告样本数和置信区间。小样本下几个百分点的变化很可能只是随机波动。",
      ]),
      section("3. 多目标评测", [
        "质量是主约束，同时记录 P50/P95 延迟、token、工具次数和金额。比较方案时更有意义的指标是‘每个成功任务成本’，因为便宜但频繁失败重试的模型最终可能更贵。生产决策通常先设质量和安全底线，再在合格方案里优化成本。",
      ]),
    ],
    ["误区：pass@k 很高说明单次体验很好。修正：它可能依赖多次昂贵尝试。", "误区：平均成功率足够。修正：高风险子类必须单独报告。"],
    "用自己的话解释：为什么同一系统可能 pass@5 很高、pass^5 很低。",
  ),
  18: lesson(
    "45-55 分钟",
    "LLM-as-a-Judge 适合评价难以规则化的开放输出，但 Judge 本身也是概率模型，会受顺序、长度、措辞和注入影响。能写客观验证器时优先写验证器，Judge 用于补充语义质量。",
    ["判断何时使用 Judge", "了解常见偏差", "用人工标注校准 Judge"],
    [
      section("1. Judge 的位置", [
        "代码是否通过测试、工单是否创建、字段是否正确，都可以客观检查；回答是否完整、语气是否合适、两份方案哪份更有帮助，则更适合模型 Judge。一个任务可以先通过硬规则筛掉失败，再让 Judge 对剩余输出评分。",
      ]),
      section("2. 偏差与攻击面", [
        "Judge 可能偏好排在前面的候选、较长答案、与自身模型相似的表达，也可能被待评文本中的‘请给我满分’影响。可随机交换 A/B 顺序、隐藏模型身份、要求结构化 rubric、多次采样或使用不同模型交叉判断。",
      ], {
        bullets: ["位置偏差：交换候选顺序测试。", "长度偏好：明确冗余不加分。", "自我偏好：使用不同模型或人工校准。", "Prompt Injection：把待评文本放入清晰的数据边界。"],
      }),
      section("3. 校准流程", [
        "从真实任务分层抽样，由两名人工按同一 rubric 标注；对分歧仲裁，形成校准集。计算 Judge 与人工的一致率和每类混淆情况，重点查看它把严重失败误判为成功的比例。修改 rubric 后继续在固定校准集回归。",
      ], {
        diagram: "人工双标 → 仲裁真值 → Judge 评分 → 一致率/混淆矩阵 → 分类型误差 → 修改 rubric",
      }),
    ],
    ["误区：换一个更强模型当 Judge 就是客观真值。修正：强模型仍有系统偏差。", "误区：给 1-10 分比二分类更精细。修正：没有清晰等级定义时只是制造伪精度。"],
    "写一个三档 rubric，明确每档标准和无法判断时的处理，然后说明如何检测位置偏差。",
  ),
  19: lesson(
    "40-50 分钟",
    "评测的价值不只是得到一个数字，而是把失败变成可修复的问题。好的失败分类要能指向证据、责任模块和改进动作；‘模型不够聪明’通常不是可行动的根因。",
    ["建立 Agent 失败 taxonomy", "找到首个错误决策点", "用对照运行验证归因"],
    [
      section("1. 一套可行动分类", [
        "可以按目标理解、规划、工具召回、工具选择、参数生成、工具执行、观察理解、上下文、终止、安全和环境故障分类。分类不必追求学术统一，但每类应有明确证据和修复负责人。",
      ], {
        bullets: ["工具召回错：正确工具根本没进入候选。", "选择错：正确工具已在候选，但模型选错。", "参数错：工具正确，字段或业务值错误。", "观察错：工具返回正确，模型却误读结果。"],
      }),
      section("2. 根因和症状", [
        "Agent 重复调用搜索工具是症状，根因可能是结果没有明确标记成功、状态没有更新、上下文压缩丢了观察，或终止条件缺失。沿 Trace 从前往后找到第一个偏离预期的点，后续错误通常只是连锁反应。",
      ]),
      section("3. 反事实验证", [
        "把疑似错误点替换成正确工具结果或正确状态，然后重放后续步骤。如果任务恢复成功，说明归因更可信；如果仍失败，还存在其他原因。修复后把样本加入回归集，并观察其他类别是否退化。",
      ], {
        diagram: "Trace → 首个偏离点 → 提出根因假设 → 注入正确中间状态重放 → 成功/失败验证",
      }),
    ],
    ["误区：最后一个报错就是根因。修正：最后报错往往是早期错误的结果。", "误区：所有失败都归为模型问题。修正：Schema、状态、工具和环境经常更容易修。"],
    "拿一条重复工具调用的轨迹，至少提出三个可能根因及各自需要查看的证据。",
  ),
  20: lesson(
    "40-50 分钟",
    "离线评测、Shadow 和 A/B 分别服务开发、真实分布验证和产品效果验证。可靠发布应该逐级扩大风险，而不是改完 Prompt 就直接把所有流量切过去。",
    ["比较三类评测", "建立发布门禁", "避免评测集污染和过拟合"],
    [
      section("1. 三层实验", [
        "离线评测在固定数据和环境中快速回归；Shadow 把真实请求复制给新版本但不返回其结果，用于观察分布和系统稳定性；A/B 才让不同用户真正看到不同版本，用业务指标判断价值。越靠后越真实，也越昂贵和有风险。",
      ], {
        diagram: "单测/离线回归 → Shadow 真实流量旁路 → 内部用户 → 小流量 A/B → 全量",
      }),
      section("2. 回归门禁", [
        "每次模型、Prompt、工具或编排变化都生成联合版本。发布前先跑确定性工具测试，再跑核心任务集和随机重复评测；质量、安全、成本和延迟任一越过阈值都应阻止发布。重要类别不能被总平均抵消。",
      ]),
      section("3. 数据污染", [
        "反复查看测试集失败并针对性修改 Prompt，本质上是在训练测试集。应保留开发集与隐藏测试集，定期引入新线上样本；评测答案也不能被写入 Agent Memory 或出现在工具可访问目录中。",
      ], {
        bullets: ["Dev：允许反复查看，用于定位和迭代。", "Test：限制访问，用于发布决策。", "线上回流：脱敏、去重、标注后加入未来回归。"],
      }),
    ],
    ["误区：离线分数高就可以上线。修正：真实流量、权限和下游波动可能完全不同。", "误区：Shadow 没风险。修正：它仍会消耗模型和工具资源，写工具必须禁用或隔离。"],
    "设计一次 Agent Prompt 改动的离线、Shadow、Canary 和回滚流程。",
  ),
  21: lesson(
    "50-60 分钟",
    "Coding Agent 是非常典型的 Harness 场景：模型只是决定搜索、读文件、编辑和运行命令；真正决定系统质量的是工作区、命令执行、测试反馈、超时和补丁管理。SWE-bench 则用真实仓库 issue 和测试来评价最终补丁。",
    ["讲清 Coding Agent 闭环", "理解 SWE-bench 的判分思路", "知道 mini-swe-agent 值得观察什么"],
    [
      section("1. Coding Agent 的闭环", [
        "Agent 读取 issue 和仓库，搜索符号或文件，查看实现和测试，编辑补丁，运行目标测试，再根据错误继续修改。Harness 管理隔离工作区、Shell、命令超时、输出截断、diff 和最终提交。测试是最重要的外部 Verifier。",
      ], {
        diagram: "issue → search/read → form hypothesis → edit patch → run tests → observe failure → edit... → final diff",
      }),
      section("2. SWE-bench 在测什么", [
        "SWE-bench 从真实 GitHub issue 构造任务，固定仓库基线和执行环境。Agent 只看到问题和仓库，提交补丁后运行测试；目标失败测试转为通过是 fail-to-pass，原本通过的测试继续通过是 pass-to-pass。环境复现与测试泄漏会直接影响成绩可信度。",
      ]),
      section("3. 阅读 mini-swe-agent 的抓手", [
        "不要急着看每行代码，先寻找四个问题：模型在哪里被调用，Shell/工具怎样执行，历史如何追加，循环如何终止。然后再观察命令超时、输出截断、环境变量和最终 patch 是怎样处理的。你会发现最小实现很短，生产能力主要长在边缘路径上。",
      ], {
        bullets: ["定位错误：是否找到正确文件和测试？", "修改质量：diff 是否最小且符合仓库约束？", "验证质量：是否运行了正确测试并防止回归？", "系统边界：命令、网络、文件和资源是否被隔离？"],
      }),
    ],
    ["误区：最终测试通过就代表完美。修正：还要检查测试泄漏、过拟合和无关改动。", "误区：Coding Agent 的核心只是更长 Prompt。修正：环境与反馈回路同样关键。"],
    "打开 mini-swe-agent，只定位模型调用、命令执行、历史更新和终止四处代码，并画成图。",
  ),
  22: lesson(
    "40-50 分钟",
    "Agent Trace 要能还原一次任务在每个模型和工具调用上的时间、输入摘要、状态变化和成本。指标告诉你系统哪里变差，Trace 帮你找到某个任务为什么变差，日志再提供具体错误细节。",
    ["设计 Trace 层级", "区分日志、指标与 Trace", "观测无进展和敏感信息"],
    [
      section("1. Span 树", [
        "根 span 对应一项用户任务；子 span 对应 Agent step、模型调用、工具调用、检索、审批和重试。所有 span 带 trace id，并记录模型/Prompt/工具版本、延迟、token、状态和错误。并行工具调用会形成兄弟 span。",
      ], {
        diagram: "agent_task trace\n ├─ step_1 / model_call\n ├─ step_2\n │   ├─ tool_search\n │   └─ tool_database\n └─ step_3 / model_call / final",
      }),
      section("2. 三种观测信号", [
        "指标用于聚合成功率、P95 延迟、成本和错误率；Trace 用于还原单次跨模块时序；日志记录离散事件和诊断上下文。常见排障顺序是指标发现异常、Trace 定位阶段、日志查看错误详情。",
      ]),
      section("3. 行为指标与脱敏", [
        "无进展可以通过重复工具+参数、连续相似观察、工作状态不变化和预算持续消耗检测。Prompt 和工具结果可能包含密钥与隐私，Trace 应保存摘要、哈希或受控引用，对敏感字段脱敏并设置访问权限。",
      ], {
        bullets: ["任务级：成功、总延迟、总成本、结束原因。", "步骤级：决策类型、状态差异、重试和工具结果码。", "版本级：模型、Prompt、工具 Schema、Runtime commit。"],
      }),
    ],
    ["误区：把完整 Prompt 全写日志最方便。修正：观测系统会成为新的数据泄露面。", "误区：只有错误才需要 Trace。修正：成功轨迹也是建立基线和比较版本的重要样本。"],
    "为一次包含两个并行工具的任务画 span 树，并列出每层至少五个字段。",
  ),
  23: lesson(
    "40-50 分钟",
    "分钟级或小时级 Agent 不应绑在一条 HTTP 连接或一个进程上。它需要 Job 模型、持久化状态机和 checkpoint，并正确处理取消、重放与外部副作用。",
    ["理解异步 Job 架构", "区分 Event Log、Snapshot、Checkpoint", "设计取消和补偿"],
    [
      section("1. 请求与任务解耦", [
        "API 创建任务后立即返回 job id，Worker 从队列取得任务并推进状态。客户端通过查询、SSE 或通知获取进度。任务等待审批时不占用 Worker；进程重启后其他 Worker 可以从持久化 checkpoint 继续。",
      ], {
        diagram: "Client → API → Job DB / Queue → Worker → Model & Tools\n   ↑         job_id        ↓\n   └──── status / SSE / notification",
      }),
      section("2. 三种持久化概念", [
        "Event Log 记录发生过的每个状态变化，适合审计和重放；Snapshot 保存某一时刻完整状态，恢复速度快；Checkpoint 表示系统确认可以安全继续的位置，通常引用最新 snapshot 和之后的 events。三者可以组合，而不是互相替代。",
      ]),
      section("3. 取消与补偿", [
        "取消信号要从 Job 传播到等待队列、模型请求和工具进程。尚未执行的步骤可以停止，已经发生的支付、发信等副作用无法靠杀进程撤销，需要执行补偿动作或转人工。状态应区分 cancelling、cancelled、failed 和 compensated。",
      ]),
    ],
    ["误区：任务长就把 HTTP timeout 调大。修正：连接和进程仍可能中断，也无法等待人工。", "误区：恢复就是从头重跑。修正：副作用会重复，应从 checkpoint 和执行记录继续。"],
    "设计一个可等待 24 小时审批的 Agent Job，说明创建、暂停、恢复、过期和取消状态。",
  ),
  24: lesson(
    "40-50 分钟",
    "Agent 会放大流量：一次用户请求可能触发十次模型调用和多个并行工具。容量控制必须同时看用户任务、Agent step 和下游工具，而不是只限制入口 QPS。",
    ["识别三层并发", "设置多维任务预算", "理解背压、队列和熔断"],
    [
      section("1. 三层并发", [
        "全局任务并发决定系统同时处理多少用户目标；单租户并发防止某个用户占满系统；工具并发保护数据库、搜索和第三方 API。单个任务内部并行还会继续放大下游流量，因此需要层级信号量。",
      ]),
      section("2. 多维预算", [
        "常见预算包括 max_steps、总时间、token、金额、工具次数和资源用量。每个昂贵步骤执行前检查剩余预算，并保留用于收尾或生成进度总结的余量。预算耗尽时应返回已完成部分，而不是突然丢失全部工作。",
      ], {
        bullets: ["硬上限：绝不允许越过，例如费用和危险工具次数。", "软上限：接近时切小模型、减少候选或请求用户扩展。", "租户配额：防止单个用户消耗全部共享容量。"],
      }),
      section("3. 过载保护", [
        "下游变慢时，有限队列和并发限制形成背压；无限队列只会把故障变成更长延迟。连续错误时熔断，暂时拒绝新调用；经过冷却进入半开状态，用少量请求探测恢复。重试必须服从同一预算和熔断策略。",
      ], {
        diagram: "admission control → bounded queue → concurrency limit → downstream\n                         ↑                    ↓\n                    reject/ETA         retry budget / circuit breaker",
      }),
    ],
    ["误区：入口只收到 10 QPS，系统压力很小。修正：每个 Agent 可能放大成几十个内部调用。", "误区：排队总比拒绝好。修正：超过时效的任务排队只会浪费资源。"],
    "估算 100 个并发任务、平均 8 steps、每步 0.4 次工具调用时的模型和工具调用规模。",
  ),
  25: lesson(
    "40-50 分钟",
    "Agent 优化是质量、延迟和成本的多目标问题。正确顺序是用失败分类找到瓶颈，再选择模型路由、候选工具缩减、上下文压缩、缓存或并行；不能只因为 token 变少就宣布优化成功。",
    ["设计模型路由", "理解常见成本手段的边界", "用固定 Eval 验证优化"],
    [
      section("1. 模型路由", [
        "可按任务类型、风险、上下文长度和置信度路由：简单抽取使用小模型，复杂规划或高风险动作使用强模型，小模型失败再升级。路由器本身也会犯错，因此要评测错误路由率、升级比例和最终成功率。",
      ]),
      section("2. 优化手段不是免费午餐", [
        "响应缓存会遇到权限和状态过期；批处理提高吞吐但增加等待；上下文压缩可能丢信息；并行工具降低墙钟时间却增加瞬时负载。每种优化都要写出命中条件和失效场景。",
      ], {
        bullets: ["工具选择错：先改候选与描述，不急着换最大模型。", "上下文成本高：结构化状态、摘要和按需检索。", "独立工具慢：安全条件满足时并行。", "简单任务占比高：小模型路由，失败再升级。"],
      }),
      section("3. 每成功任务成本", [
        "方案 A 每次 0.1 元但成功率 50%，方案 B 每次 0.15 元但成功率 90%；如果失败需要重试，B 可能更便宜。评测应固定任务集，多次运行，同时报告成功率、P95 延迟和总成本，先守住质量与安全底线。",
      ]),
    ],
    ["误区：小模型单次便宜，所以系统成本一定低。修正：失败和升级会改变总成本。", "误区：Prompt 越短越好。修正：删掉关键约束会用失败换 token。"],
    "针对‘工具选择准确但参数经常错’提出三种优化，并设计对照指标。",
  ),
  26: lesson(
    "40-50 分钟",
    "多 Agent 的合理价值来自明确分工、并行探索或独立验证，而不是把一个 Prompt 拆成多个角色。它会引入更多模型调用、通信、状态同步和失败归因，默认应先尝试单 Agent 加工具。",
    ["判断何时需要多 Agent", "理解 Supervisor 模式", "设计最小必要的信息共享"],
    [
      section("1. 收益与成本", [
        "研究任务可以让多个 Agent 并行搜索不同来源，代码任务可以让实现者和 Verifier 分离；这类场景有真实收益。如果任务只是顺序调用三个工具，多 Agent 只会增加角色消息和延迟。",
      ]),
      section("2. Supervisor 模式", [
        "Supervisor 根据任务状态把子任务分给专业 Agent，收集结构化产出，处理冲突并决定终止。它可能成为瓶颈和单点，因此简单路由可由确定性代码完成，只有开放分配才交给模型。",
      ], {
        diagram: "                 ┌→ Research Agent ─┐\nUser → Supervisor ├→ Coding Agent ───┼→ aggregate / verify → final\n                 └→ Review Agent ───┘",
      }),
      section("3. 信息和权限隔离", [
        "不要把所有 Agent 的完整聊天互相广播。通过结构化任务、产出物引用和共享状态传递最少必要信息；每个 Agent 只获得职责所需工具。这样既减少上下文，也限制一个受注入 Agent 影响整个系统。",
      ]),
    ],
    ["误区：给同一模型不同角色 Prompt 就获得多个专家。修正：角色不自动创造能力和独立性。", "误区：Agent 越多覆盖越全面。修正：通信成本和错误组合也会同步增加。"],
    "为一个任务说明为什么单 Agent 不够，并量化多 Agent 带来的并行或验证收益。",
  ),
  27: lesson(
    "45-55 分钟",
    "更偏算法的 Agent 优化通常围绕生成候选、Verifier 评分和搜索策略展开。搜索可以提高成功上限，但成本会随候选和分支快速增长，而且 Verifier 不可靠时可能稳定选错。",
    ["理解 Generator-Verifier", "比较 Best-of-N、Beam 与 Tree Search", "判断何时需要训练"],
    [
      section("1. Generator-Verifier", [
        "Generator 产生一个或多个计划、答案或补丁；Verifier 使用测试、规则、奖励模型或 LLM 评分；系统选择最佳候选，或把反馈交回 Generator 修正。关键不是生成数量，而是候选要有多样性，Verifier 要与真实目标一致。",
      ]),
      section("2. 三种搜索", [
        "Best-of-N 独立采样 N 个完整候选后选优，实现最简单；Beam Search 每层保留若干高分前缀；Tree Search 展开动作树并依据价值选择分支。Agent 搜索还要管理工具副作用和环境快照，远比纯文本搜索昂贵。",
      ], {
        bullets: ["Best-of-N：并行简单，不能复用中间成果。", "Beam：逐层剪枝，可能过早丢掉后期更好的路径。", "Tree：探索充分，分支与环境复制成本最高。"],
      }),
      section("3. 什么时候训练", [
        "先通过稳定评测证明错误来自模型策略，而不是工具描述、上下文或 Runtime。积累足够高质量轨迹或可靠奖励后，才考虑 SFT、偏好优化或 RL。没有数据闭环时，训练只会把未理解的问题固化。",
      ], {
        diagram: "失败归因 → Prompt/Schema/Runtime 基线 → 稳定轨迹与奖励 → 训练 → 独立测试集验证",
      }),
    ],
    ["误区：生成更多候选总会更好。修正：候选同质或 Verifier 错误时只会更贵。", "误区：工具调用不准就立刻微调。修正：先检查 Schema、候选集和校验反馈。"],
    "说明 Best-of-5 为什么可能提升 pass@5，却不一定改善用户的一次成功率和成本。",
  ),
  28: lesson(
    "40-50 分钟",
    "Agent 行为由模型、Prompt、工具、编排和环境共同决定。只记录代码 commit 无法复现一次线上行为，也无法安全回滚。生产发布需要联合版本、灰度流量和状态兼容。",
    ["定义 Agent 联合版本", "设计灰度与回滚", "处理工具 Schema 演进"],
    [
      section("1. 联合版本", [
        "一个可复现版本至少包含模型及采样参数、system/developer prompt、工具 Schema 和实现、编排策略、记忆/检索配置、依赖与运行环境。Trace 必须记录这些版本，才能把成功率变化归因到具体改动。",
      ]),
      section("2. 灰度发布", [
        "经过离线回归后，先 Shadow，再内部用户和小流量 Canary。持续观察成功率、安全违规、错误率、P95 延迟和每成功任务成本；越过护栏自动停止放量。回滚不仅要保留旧代码，还要考虑新版本创建的任务状态是否能被旧版本读取。",
      ], {
        diagram: "v2 offline pass → shadow → 1% canary → 10% → 50% → 100%\n                     任一护栏失败 ───────────────→ stop / rollback",
      }),
      section("3. Schema 与长任务兼容", [
        "新增可选字段通常向后兼容，删除、重命名和语义变化需要版本化。长任务恢复时要知道当时使用哪个工具版本；可以保留旧执行器直到任务结束，或提供状态迁移。外部副作用已发生时，代码回滚不能自动撤销现实世界。",
      ]),
    ],
    ["误区：Prompt 只是配置，不需要版本。修正：Prompt 改动可以完全改变行为。", "误区：切回旧镜像就完成回滚。修正：任务状态和外部副作用可能已经不可兼容。"],
    "列出一次 Agent 发布清单，并说明新工具字段删除时如何处理正在等待审批的旧任务。",
  ),
  29: lesson(
    "50-60 分钟",
    "Agent 系统设计题的重点不是堆框架名，而是把不确定模型放进一个有边界的生产系统。固定顺序是：澄清任务与风险，画执行主链路，再覆盖状态、可靠性、安全、观测、评测和成本。",
    ["掌握系统设计答题模板", "设计桌面 Agent", "体现后端经验的迁移价值"],
    [
      section("1. 先澄清需求", [
        "询问目标用户、任务类型、工具和副作用、并发规模、延迟与成本 SLO、是否长任务、数据权限和成功标准。Agent 系统最重要的差异往往来自风险：只读研究助手与能操作桌面的 Agent 不是同一个安全等级。",
      ], {
        bullets: ["功能：要完成哪些任务，允许多少自主决策？", "规模：QPS、平均 steps、模型与工具延迟。", "风险：读写范围、不可逆动作、租户隔离。", "目标：成功率、P95、成本和可用性。"],
      }),
      section("2. 画主链路和状态", [
        "模块可拆成 API/Job、Runtime 状态机、Model Gateway、Tool Registry/Executor、Context/Memory、权限与沙箱、队列和持久化、Tracing/Eval。沿一次请求讲正常路径，再主动补工具超时、用户取消、审批等待和进程重启。",
      ], {
        diagram: "Client → API/Job → Runtime ↔ Model Gateway\n                         ↕\n             Context/State ←→ Tool Executor → Sandbox/MCP\n                         ↓\n                 Trace / Eval / Audit",
      }),
      section("3. 桌面 Agent 的特殊点", [
        "桌面内容和网页都不可信，Agent 又可能接触文件、账户和 GUI。需要按应用、路径和动作授权，高风险操作展示最终参数并确认，执行环境记录截图/DOM/工具轨迹。长操作使用 Job 和 checkpoint，发布时用真实任务回放和安全集回归。",
        "你的后端优势应落在状态机、幂等、队列、权限、观测和故障处理上；再补模型路由、上下文和 Eval，就能形成 Agent Harness 岗位需要的交叉能力。",
      ]),
    ],
    ["误区：一上来选择 LangGraph 或某云服务。修正：技术选型必须由状态、规模和风险推导。", "误区：只讲正常路径。修正：Agent 设计题最能拉开差距的是失败、安全和评测。"],
    "用 15 分钟设计一个桌面 Agent，至少主动覆盖两种故障和两种安全风险。",
  ),
  30: lesson(
    "45-60 分钟",
    "最后一天不再增加新术语，而是把 30 天内容压缩成一条可稳定口述的主链路。面试时先给结论，再讲机制、失败边界和验证方法；这比背一段很长但无法追问的答案更可靠。",
    ["形成六条知识主线", "用一个失败案例串起系统", "明确自己的岗位定位"],
    [
      section("1. 六条主线", [
        "执行：Agent Loop、规划和终止；工具：Function Calling、MCP 和路由；状态：消息、上下文、记忆与 checkpoint；可靠性：超时、重试、幂等和 HITL；安全：权限、沙箱与注入；证据：Tracing、Eval、版本和发布。任何问题先判断属于哪条线。",
      ], {
        diagram: "执行循环 ─ 工具协议 ─ 状态上下文\n   │           │           │\n可靠恢复 ─ 安全权限 ─ Trace & Eval",
      }),
      section("2. 用失败案例证明理解", [
        "选择一次工具重复调用：Trace 显示模型收到成功结果后仍再次调用。继续检查发现工具结果没有结构化 success 字段，摘要又丢失了上一轮状态。修复结果 Schema 和终止条件，加入回归样本，比较成功率、平均 steps 和成本。这条故事同时覆盖观测、归因、修复和评测。",
      ]),
      section("3. 面试表达与定位", [
        "每题使用四层结构：一句定义；核心机制；取舍或失败；如何验证。遇到没做过的技术，明确实践边界，再给出基于已有知识的设计和验证方法，不编造数据。",
        "你的定位不是从后端凭空跳到纯模型算法，而是把后端工程能力迁移到模型驱动系统，并增加 Agent 行为评测与策略优化。这是连贯且可信的路线。",
      ], {
        bullets: ["60 秒：定义 + 主链路。", "3 分钟：加入失败、安全和评测。", "追问：落到状态、工具、指标和实际取舍。"],
      }),
    ],
    ["误区：把所有名词塞进自我介绍。修正：围绕一个真实主线逐层展开。", "误区：不会就强行给确定答案。修正：说明边界、假设和验证方法更可信。"],
    "完成一次 30 分钟模拟面试：基础、可靠性、Eval、系统设计各回答至少两题。",
  ),
};

export function lessonForDay(day) {
  return lessons[day];
}
