# 第二章：三层边界

## 本章问题

你在 Agent Harness 面试中被问到的“循环”背后，通常还隐含着另一个问题：

> 这个循环里，哪些逻辑属于 Agent，哪些属于 Model，哪些属于 Environment？

如果边界答不清楚，后面的 Tool、Memory、Sandbox 和 Evaluation 都会混在一起。

## 三个对象

### Agent：组织过程

Agent 负责：

- 初始化任务消息；
- 重复执行 step；
- 记录 `messages`；
- 统计调用次数和成本；
- 检查限制；
- 处理 Runtime 控制流；
- 保存完整轨迹。

它不应该知道某个具体模型厂商的 API 细节，也不应该把 Docker 命令写死在主循环里。

源码：

```text
src/minisweagent/agents/default.py
```

### Model：把消息变成动作

Model 负责：

- 接收消息历史；
- 调用具体模型 API；
- 把模型响应解析为统一 action；
- 把 Environment 输出格式化为模型下一轮能读懂的 message；
- 处理不同供应商的消息格式；
- 序列化模型配置和统计信息。

在 v2 中，动作解析已经被放在 Model 一侧。这样 Agent 不需要知道动作到底来自原生 tool calling，还是来自文本代码块。

源码：

```text
src/minisweagent/models/litellm_model.py
src/minisweagent/models/utils/actions_toolcall.py
src/minisweagent/models/utils/actions_text.py
```

### Environment：把动作施加到世界

Environment 负责：

- 接收结构化 action；
- 确定工作目录和环境变量；
- 启动子进程、容器或远程沙箱；
- 捕获输出和返回码；
- 处理超时；
- 识别完成信号；
- 把结果返回给 Agent。

源码：

```text
src/minisweagent/environments/local.py
```

## 用接口看边界

`src/minisweagent/__init__.py` 使用 Protocol 描述三个对象需要提供的能力：

```python
class Model(Protocol):
    def query(self, messages: list[dict[str, str]], **kwargs) -> dict: ...
    def format_message(self, **kwargs) -> dict: ...
    def format_observation_messages(
        self, message: dict, outputs: list[dict], template_vars: dict | None = None
    ) -> list[dict]: ...
    def serialize(self) -> dict: ...


class Environment(Protocol):
    def execute(self, action: dict, cwd: str = "") -> dict[str, Any]: ...
    def serialize(self) -> dict: ...


class Agent(Protocol):
    def run(self, task: str, **kwargs) -> dict: ...
    def save(self, path: Path | None, *extra_dicts) -> dict: ...
```

这里的关键不是记住 Protocol 的语法，而是理解“依赖能力，不依赖继承”：

> 只要一个对象提供了 Agent 所需的方法，它就可以被接入。

这就是 duck typing 在 Runtime 里的实际价值。你可以写一个 `FakeModel`、`RecordingEnvironment` 或 `DockerEnvironment`，不必先继承一个庞大的基类。

## 为什么 v2 把 action parsing 放在 Model

考虑两种模型输出：

### 原生 tool calling

模型返回：

```json
{
  "tool_calls": [
    {
      "id": "call_1",
      "function": {
        "name": "bash",
        "arguments": "{\"command\":\"ls\"}"
      }
    }
  ]
}
```

### 文本代码块

模型返回：

````text
```mswea_bash_command
ls
```
````

两种输出的解析规则明显不同，但 Agent 的任务完全相同：

```text
得到 action -> 交给 Environment -> 得到 output -> 追加 observation
```

因此 v2 的做法是：

```text
Model 负责 provider-specific parsing
Agent 只消费统一的 action dict
Environment 只消费 action dict
```

这样切换解析方式时，不需要重写主循环。

## 一次动作的统一形状

在 tool calling 路径中，`parse_toolcall_actions()` 最终生成类似：

```python
{
    "command": "ls",
    "tool_call_id": "call_1",
}
```

Environment 不需要知道这个动作原来来自哪家 API。它只读取：

```python
command = action.get("command", "")
```

这就是一个好的边界：上游可以变化，下游接口不变。

## 源码地图

| 层 | 入口 | 关键方法 |
| --- | --- | --- |
| Agent | `agents/default.py` | `run`, `step`, `query`, `execute_actions` |
| Model | `models/litellm_model.py` | `query`, `_parse_actions`, `format_observation_messages` |
| Parser | `models/utils/actions_toolcall.py` | `parse_toolcall_actions` |
| Environment | `environments/local.py` | `execute`, `_check_finished`, `_run` |
| Run script | `run/mini.py` | 组装 config、model、env、agent |

## 可运行实验：替换 Environment

先读 `tests/agents/test_default.py` 中的 `LocalEnvironment()`。然后思考：如果我们把 Environment 换成只记录命令、不真正执行命令的对象，Agent 还需要改吗？

写出下面这个最小对象，放在临时 Python 文件中：

```python
class RecordingEnvironment:
    def __init__(self):
        self.commands = []

    def execute(self, action, cwd=""):
        self.commands.append(action["command"])
        return {"output": "recorded", "returncode": 0, "exception_info": ""}

    def get_template_vars(self, **kwargs):
        return {}

    def serialize(self):
        return {"info": {"environment_type": "recording"}}
```

然后回答：

1. `DefaultAgent` 是否需要知道 `RecordingEnvironment` 的类名？
2. `execute_actions()` 是否需要修改？
3. 如果这个 Environment 永远不抛出 `Submitted`，Agent 如何结束？

第三个问题会把你带到第四章：能力边界和控制流边界不是一回事。

## 面试问答

### Q1：为什么不让 Agent 直接调用 LiteLLM？

因为 Agent 的核心是控制流，不是供应商适配。把 API 调用、消息格式、成本计算、tool parsing 放入 Model，可以让同一套 Agent 复用不同模型后端。

### Q2：为什么 Environment 的输入是 dict，而不是直接传字符串？

因为 dict 可以携带命令之外的元数据，例如 `tool_call_id`、未来的权限信息、工作目录或动作类型。当前 LocalEnvironment 主要读取 `command`，但边界已经留下扩展空间。

### Q3：Protocol 和抽象基类的区别是什么？

Protocol 主要描述“需要哪些方法”，不强制继承关系。对可插拔 Runtime 来说，这能减少耦合，方便测试替身、容器环境和第三方实现接入。

## 闭卷检查

不看源码，完成下面的判断：

- “Model 执行 Bash”对不对？为什么？
- “Agent 解析所有厂商响应”对不对？为什么？
- “Environment 负责把 output 格式化成下一轮模型消息”对不对？为什么？
- 原生 tool calling 和文本代码块解析不同，为什么不需要两套 Agent？
- 如果把 LocalEnvironment 换成 DockerEnvironment，主循环应该保持不变吗？

### 完成标准

你能用一句话说清楚：

```text
Agent 管过程，Model 管语言模型接口与消息转换，Environment 管外部世界。
```

并且能指出 action parsing 位于 Model 一侧，而不是 Agent 一侧。

