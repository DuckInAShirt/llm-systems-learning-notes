# 第四章：完成、限制与异常控制流

## 本章问题

你之前对这条链已经有一个关键误区：

```text
模型发出完成命令
-> 循环自动知道完成
```

中间其实还有明确的识别和传播过程。本章把它拆成三问：

1. 谁发出完成信号？
2. 谁识别完成信号？
3. 谁让主循环退出？

## 完成链路的准确答案

```text
Model
  -> 提出 bash action：
     echo COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT
  -> LocalEnvironment._run()
  -> LocalEnvironment._check_finished()
  -> raise Submitted(exit_message)
  -> DefaultAgent.run() 捕获 InterruptAgentFlow
  -> add_messages(exit_message)
  -> 检查 messages[-1]["role"] == "exit"
  -> break
```

因此：

- **模型发出信号**；
- **Environment 识别信号**；
- **`run()` 根据 exit message 结束循环**。

这三个角色必须分开说。面试时如果只说“模型完成后循环退出”，答案是不完整的。

## `Submitted` 从哪里来

`LocalEnvironment._check_finished()` 先把输出按行拆开：

```python
lines = output.get("output", "").lstrip().splitlines(keepends=True)
if lines and lines[0].strip() == "COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT" and output["returncode"] == 0:
    submission = "".join(lines[1:])
    raise Submitted(
        {
            "role": "exit",
            "content": submission,
            "extra": {"exit_status": "Submitted", "submission": submission},
        }
    )
```

有两个细节：

1. 忽略开头空白后，完成标记必须是成功命令输出的第一行；
2. 标记之后的内容会被当作最终提交内容。

所以 Prompt 才会要求模型把完成命令单独执行，不要和别的命令混在一起。

## 为什么使用异常

假设 Environment 的调用层级是：

```text
run
  -> step
      -> execute_actions
          -> env.execute
              -> _check_finished
```

如果不用异常，完成状态需要逐层返回：

```python
result = env.execute(action)
if result["finished"]:
    return result
...
```

每一层都要理解 `finished`，每一层都要把它继续向上传递。未来还会加入：

- 用户中断；
- 成本超限；
- 时间超限；
- 格式错误；
- 沙箱错误；
- 重试信号。

异常提供了一个更合适的机制：

> 深层组件发现必须改变外层控制流时，直接抛出携带消息的控制流异常，由最外层统一处理。

在 mini 中，控制流异常的基类是：

```python
class InterruptAgentFlow(Exception):
    def __init__(self, *messages: dict):
        self.messages = messages
        super().__init__()
```

它的子类包括：

```text
Submitted
LimitsExceeded
TimeExceeded
UserInterruption
FormatError
```

这些异常不是“程序坏了”的普通错误。它们是带有 Runtime 语义的跳转信号，并且携带要加入 trajectory 的 message。

## `run()` 如何处理异常

核心逻辑可以简化成：

```python
while True:
    try:
        self.step()
    except FormatError as e:
        # 把格式错误反馈给模型，允许下一轮修正
        self.add_messages(*e.messages)
    except InterruptAgentFlow as e:
        # Submitted、LimitsExceeded 等会把退出消息加入 history
        self.add_messages(*e.messages)
    except Exception as e:
        # 未知异常：记录后重新抛出
        self.handle_uncaught_exception(e)
        raise

    if self.messages[-1].get("role") == "exit":
        break
```

`FormatError` 很特殊：不是每次都退出。默认最多允许连续三次格式错误，错误 message 会回到上下文，让模型尝试修正。超过限制后，才追加 `role="exit"` 的 `RepeatedFormatError`。

这体现了一个重要 Runtime 模式：

```text
可恢复错误 -> 变成 Observation / feedback，继续循环
不可恢复错误 -> 变成 exit message，结束循环
```

## 四类异常的语义

| 异常 | 触发者 | 默认后果 |
| --- | --- | --- |
| `Submitted` | Environment 识别完成命令 | 追加提交结果，结束 |
| `LimitsExceeded` | Agent 的 limit 检查 | 追加退出消息，结束 |
| `TimeExceeded` | Agent 的 wall-time 检查 | 追加退出消息，结束 |
| `FormatError` | Model 解析输出失败 | 反馈给模型；连续超限后结束 |
| `UserInterruption` | 交互层或用户 | 追加退出消息，结束 |

## 失败命令为什么不一定抛异常

例如：

```bash
nonexistent_command_12345
```

它通常会返回：

```python
{
    "output": "... command not found ...",
    "returncode": 127,
    "exception_info": "",
}
```

这不是 Runtime 级别的中断，而是任务世界中的失败。模型应当看到这个结果，然后决定是否换一个命令。

因此要分清：

```text
任务动作失败：通常是 observation
Runtime 无法继续：通常是 exception / exit
```

## 限制检查的时机

`query()` 在真正发起模型调用之前检查：

- `step_limit`；
- `cost_limit`；
- `wall_time_limit_seconds`。

这意味着达到限制后，不会再额外调用一次模型来“询问是否退出”。它直接抛出 `LimitsExceeded` 或 `TimeExceeded`，由 `run()` 结束。

这种设计避免了：

- 超限后又产生一笔模型费用；
- 处于死循环的 Agent 继续请求；
- 任务在失控时没有硬停止边界。

## 可运行实验：观察三种退出

执行：

```bash
cd /Users/xinranzhao/Documents/llm-study/books/mini-swe-agent-source-guide/mini-swe-agent
pytest -q tests/agents/test_default.py -k 'successful_completion or step_limit_enforcement or timeout_handling'
```

然后分别定位：

- `test_successful_completion`：`Submitted`；
- `test_step_limit_enforcement`：`LimitsExceeded`；
- `test_timeout_handling`：Environment 返回超时结果，但模型仍然可以继续并完成任务。

注意第三个测试：命令超时后，`LocalEnvironment.execute()` 捕获了底层 `TimeoutExpired`，把它整理成 observation，而不是直接让整个 Agent 退出。这和“wall time limit”是两种不同的时间控制：

```text
单个命令超时 -> action 级 observation
整个 Agent 超时 -> TimeExceeded，退出
```

## 设计取舍：异常不是越多越好

异常控制流适合表达“必须跳到外层处理”的信号，但不应该把所有普通分支都改成异常。

适合抛异常：

- Environment 深层识别完成；
- Agent 在 query 前检测到硬限制；
- Model 无法解析必须反馈；
- 用户中断了交互。

不适合抛异常：

- 命令返回非零；
- 普通的空输出；
- 模型正常提出下一步 action；
- 一般的统计更新。

判断标准不是“异常快不快”，而是：

> 这个事件是普通业务结果，还是需要穿透多个调用层改变外层循环？

## 面试问答

### Q1：谁识别完成命令？

Environment。当前 LocalEnvironment 在 `_check_finished()` 中检查命令输出的第一行，满足条件后抛出 `Submitted`。

### Q2：谁真正退出循环？

`DefaultAgent.run()`。它捕获 `InterruptAgentFlow`，把携带的 exit message 加入 `messages`，然后根据最后一条消息的 `role == "exit"` 跳出循环。

### Q3：为什么不让 Model 直接返回 `done=True`？

因为模型的文本或 tool call 只是意图，是否真的完成需要由执行环境观察。模型可能声称完成，但命令失败；Environment 看到真实 returncode 和 output 后才有资格确认。

### Q4：`FormatError` 和命令失败有什么区别？

`FormatError` 表示模型输出无法被 Runtime 解析为合法 action，需要把错误反馈给模型；命令失败表示 action 已经合法执行，只是外部世界返回了非零结果。

### Q5：异常里为什么要带 message？

这样深层组件可以把结构化的退出结果或错误反馈直接交给最外层，而不需要每一层额外设计返回值和状态传递协议。

## 闭卷检查

请不看源码，回答下面四句话：

1. 模型发出完成信号之后，谁检查它？
2. 谁创建 `role="exit"` 的消息？
3. 谁捕获 `Submitted`？
4. 为什么 `run()` 能通过最后一条消息判断退出？

### 完成标准

你能够完整复述：

```text
模型只提出完成意图；
Environment 观察真实命令输出并识别完成；
Environment 抛出携带 exit message 的 Submitted；
run() 捕获 InterruptAgentFlow，把消息追加到 history；
最后一条消息 role=exit，所以主循环 break。
```
